# Chewo — notes for agents

Conventions and gotchas discovered while working in this repo. Loaded into
every session via `@AGENTS.md` from `CLAUDE.md`.

## Learned

- 2026-07-18: Our umbrella term for one item in the tab bar is **"session"** (what you create/focus/close). Layers keep code-level names: **tab** = UI chip, **terminal** = `termId` pty runtime, **conversation** = `sessionId` CLI transcript. Vocabulary only — don't mass-rename identifiers unless asked.
- 2026-07-17, reversed 2026-08-03, **reversed back 2026-08-07**: Martin always runs multiple agents concurrently in the same repo, and never wants "you have 2 agents here" warnings — that part has never changed. The default has moved twice. Isolation was opt-in per terminal, went **on by default** on 2026-08-03 because Ship's one-click `git add -A` in a shared checkout stages whatever every other live agent has touched, and is **opt-in again as of 2026-08-07**: a new session now starts in the project's own checkout (`branchMode: 'current'` in `newAgent`, App.tsx). Most sessions are a question or a small edit on the branch already open, and cutting a worktree for one leaves a folder to reap. The Ship hazard is unchanged and is now the *reason to reach for the toggle* rather than a reason to force it on everyone — the composer's setup-row switch is the way in, and its off-state title still says Ship here stages every agent's work at once. Everything downstream of the flag is untouched: the worktree is still cut on the first message, still named after the task, and still reaped when its PR merges. Neither the play button nor the `+` shell is pinned to the main checkout — see the 2026-08-03 entries below.
- 2026-07-18: The UI/UX overhaul canon lives in `design/` — read `design/README.md` for the persona-pass process and `design/06-chosen-direction.md` for Martin's LOCKED visual direction. Direction is **Graphite base + Emerald accent** (the warm "Kiln"/terracotta values in `design/02–04` were rejected as "mustard brown"; 06 supersedes their hex, the token architecture in 04 still stands). HTML mockups in `design/mockups/`.
- 2026-07-19: For draggable UI (code tabs, cards), use native HTML5 DnD (`draggable` + `onDragStart`/`onDragOver`/`onDrop`/`onDragEnd`), matching `TodoBoard.tsx`. Critical gotcha: **never put `user-select: none` on the draggable element** — on this macOS Chromium/Electron it silently suppresses native drag initiation (element goes inert: no drag, no text selection). Native drag cancels text selection on its own, so it isn't needed. Dead ends that were tried and reverted: native DnD *with* `user-select: none` (cursor changes, nothing drags) and a pointer-events/`setPointerCapture` rewrite (highlights tab text instead of dragging).
- 2026-07-21: Second way to kill native drag in the same component: **never mount, unmount, or resize an element in the drag source's layout flow from `onDragStart`**. The T5 run strip rendered as `{draggingId && <strip/>}` above the columns — the state change reflowed the grid under the card mid-`dragstart` and the drag silently never began (symptom identical to the `user-select` bug: card feels inert). If such an affordance is ever needed again, keep it **always mounted and absolutely positioned**, toggling only `opacity`/`pointer-events`, which don't reflow — that fix worked, though the strip itself was then dropped for a modal button. Toggling a class on the dragged card itself (e.g. `.todo-card-dragging { opacity }`) is fine.
- 2026-07-29: **There is no Claude SDK in this repo** — no `@anthropic-ai/*` dependency exists. Every AI feature shells out to a CLI through `/bin/zsh -ilc`. The three headless ones (notes structuring, notes Q&A, todo voice) are **agent-agnostic**: pick the CLI per feature in Settings → Agents, stored in `userData/settings.json` under `agents`, read in main via `agentFor(task)`. **Add per-agent divergence only in `src/main/agent-runner.ts`** (`runAgentText` / `runAgentJson` / `chatCommand` / `normalizeChatEvent`) and describe the agent in `src/shared/agents.ts` — feature code must never branch on the agent. Gotchas that already bit: Codex's `--output-schema` is **OpenAI strict mode** (every property in `required`, `additionalProperties: false` — `toStrictSchema()` handles it) while Claude's `--json-schema` is looser; Codex emits whole `item.completed` messages, **not token deltas**, so `chat_text` carries a `delta` flag; Codex needs `--skip-git-repo-check` because the notes root and `~/.chewo` aren't repos. Never forward a CLI's raw event schema to the renderer — normalize to `ChatEvent` in main first. **Model lists are never hardcoded**: Claude uses its `--model` *aliases* (`opus`/`sonnet`/`haiku`/`fable`), which always resolve to the latest model of a tier; Codex is enumerated at runtime from `codex debug models` (`src/main/agent-models.ts`) because its catalog is server-driven and genuinely changes between calls. Reasoning effort is **per model, not per agent** — read it off the selected model, never a global list.
- 2026-07-29: **Interactive spawns are agent-agnostic too** — anything that launches a session (card runs, worktrees, new-session) must offer both CLIs, never hardcode `source: 'claude'`. Both take the prompt as a **positional argv** and auto-submit it (`claude [flags] '<prompt>'` / `codex [flags] '<prompt>'`); the divergence lives in `promptFlags()` in `src/main/terminals.ts` and is images only. Claude reads image *paths* found in the prompt, so it just needs the assets folder unlocked with `--add-dir`; **Codex has no read-side `--add-dir`** (its flag grants *write* roots) and its file tools can't read a PNG, so files are attached with `-i <file>` per image. Both flags are **variadic**, so the prompt always goes first and flags last — reversed, the CLI parses the prompt as another value and starts with no prompt at all.
- 2026-07-29: **Our MCP server is named `chewo`** (`packages/chewo-mcp`, package `@chewo/mcp`, tools reach models as `mcp__chewo__*`); the old `context-bridge` name survives only as `LEGACY_MCP_SERVER_NAME` for migration. It **ships inside the app** as an extraResource (`Resources/bin/chewo-mcp.cjs`) — never assume the user installs or registers it separately, and never register it with `node <path>`: Claude Code is a native binary and Codex is Rust, so a user can have both CLIs and no Node. The command is always **Chewo's own binary with `ELECTRON_RUN_AS_NODE=1`**. Registration is **opt-in in Settings → Connections**; `reconcileMcpServer()` at launch only repairs a registration the user already made (packaged builds only — in dev it would fight the installed app over the same config entries). Reads parse `~/.claude.json` / `~/.codex/config.toml` directly, **writes always shell out to `claude mcp add|remove` / `codex mcp add|remove`** (same doctrine as `mcp-writer.ts`). Server state is `~/.chewo/mcp/` (inbox + audit.log). Renderer-safe names/types live in `src/shared/chewo-mcp.ts`, fs paths in `src/shared/mcp-paths.ts` — keep node imports out of the former.
- 2026-07-29: **Dictation streams to Deepgram; the Swift sidecar is capture-only.** `packages/audio-capture` (`chewo-audio-capture`) opens the mic or a Core Audio process tap and writes **16 kHz mono Int16 to fd 3** — nothing else. It has no package dependencies and knows nothing about transcription. Main owns the Deepgram connection (`src/main/deepgram.ts`, `listen.v1`), so the API key never reaches the renderer; it lives in `userData/credentials.json` encrypted via Electron `safeStorage` (`src/main/credentials.ts`), deliberately **not** in `settings.json`, which the renderer rewrites wholesale. Traps that already bit: **WebSocket booleans must be strings** (`interim_results: 'true'`) while the *batch* endpoint takes real booleans; **`stopped` is emitted only after the sidecar drains fd 3**, so it means "audio complete" and is what lets main close the stream without clipping words; **`nova-3` is a wire alias Deepgram accepts but never lists** in `GET /v1/models` (the catalog says `nova-3-general`), and storing an unlisted id left the picker with no matching row, which silently drifted the user's model — `normalizeStt` migrates the aliases, and **`Select` must never fall back to `options[0]`** when the value is missing or it displays a setting the user never chose. The model picker offers **two curated tiers**, not the catalog's 41 models across ~400 rows (one row per model *version*, which also handed React duplicate keys and broke filtering): Nova-3 monolingual ($0.0048/min) and multilingual ($0.0058/min), the latter being the *same* `nova-3-general` with `language=multi`. Flux is excluded — it needs `listen.v2` and a different message protocol. Every capture is also written to `userData/recordings/<id>.pcm` **synchronously** (a buffered stream loses its buffer when the app dies, which is the exact case the file exists for); a clean `final` deletes it, anything else leaves it recoverable via 5-minute WAV chunks through the pre-recorded API.
- 2026-07-29: **`npm install <package>` breaks terminals; `npm install` with no args does not.** npm strips the executable bit off files inside a package (it only preserves it for declared `bin` entries), and node-pty's prebuilt `spawn-helper` needs it — without it `terminal:create` dies with `posix_spawnp failed`. The repo's root `postinstall` (`chmod +x node_modules/node-pty/prebuilds/*/spawn-helper`) repairs this, but it **only runs on a bare `npm install`**. A targeted `npm install <pkg>` re-extracts node-pty, re-strips the bit, and skips the root `postinstall` — verified both ways, including an install that reported "up to date" and still broke it. **After adding any dependency, run `npm run postinstall`.**
- 2026-07-29: **A missing runtime dependency produces a build that succeeds and an app that dies on launch.** electron-vite leaves `dependencies` as bare `import`s in `out/main/index.js` (it never bundles or resolves them), and electron-builder copies prod deps out of `node_modules` — so a package listed in `package.json`/`package-lock.json` but absent from `node_modules` is silently skipped by *both*, and the packaged app throws `ERR_MODULE_NOT_FOUND` at startup. This is what a pull that adds a dep looks like when `npm install` wasn't re-run (it bit `@deepgram/sdk` after the Deepgram merge). **Run `npm install` after any pull/merge that touches `package.json`**, and if a packaged build reports a missing module, check `node_modules/<pkg>` before suspecting the packaging config.
- 2026-07-29: **Worktree truth comes from git, not from `projects.json`.** The sidebar's "Isolated branches" group is reconciled against `git worktree list --porcelain` (`listWorktrees` in `src/main/worktrees.ts`), because the records drift: dev and packaged builds keep **separate** `projects.json` files, and closing a pane deletes the `SavedTerminal` that was the branch's only handle. Two gotchas if you touch this. (1) **"0 commits ahead of main" cannot tell a fresh worktree from a merged one** — both sit at zero. Anything that decides a branch is done must compare the tip against the commit it was *cut at* (`Worktree.baseCommit`, recorded at creation, recovered from the branch reflog's `branch: Created from X` entry when adopting), and must treat unknown provenance as still-live: locking a fresh worktree is far worse than leaving a spent one open. Uncommitted changes always keep a branch live. A squash/rebase merge rewrites SHAs and is deliberately *not* detected — the merge modal's **Mark as done** sets `Worktree.doneAt`, the hand override for exactly that, undoable from the row. (2) The `Row` primitive (`components/ui/Row.tsx`) **paints its own live dot** from the `live` prop — passing a `<Dot tone="live"/>` as `leading` renders a second one.
- 2026-07-29: **`git status` collapses a wholly-untracked directory into one entry with a trailing slash** (`? packages/foo/`), so a status entry is not always a file. Diffing one is a trap: `git diff --no-index -- /dev/null <dir>/` resolves `/dev/null`'s *basename* inside the directory and dies with `error: Could not access '<dir>/null'` — the panel's "Could not access …/null" bug. `ChangedFile.isDir` now flags these, `gitDiff` refuses them, and `GitPanel` expands the row via `gitUntrackedFiles()` (`git ls-files --others --exclude-standard`, capped at 500). **Do not "fix" this by switching status to `-uall`** — one unignored build dir turns 1 row into 517 (measured on this repo against a stale 318 MB Swift `.build/`, since deleted).
- 2026-07-30, **narrowed 2026-08-03**: **`src/main/git.ts` is read-only by contract; every git *mutation* lives elsewhere, split by blast radius** — `git-ops.ts` (bring commits in), `git-ship.ts` (send work out), `worktrees.ts` (checkout lifecycle). Keep that split so "does this touch the repo?" is answerable by filename. Same house rules everywhere: never `--force`, never `-D`, never stash, and pass git's own message through verbatim. **`git-ops.ts` was a whole branch menu — list/switch/create/fetch/pull/push — and all of it went when sessions became worktrees**: you don't switch branches in a session's checkout (you start another session), push is Ship's job, and fetch only existed to serve those two. What is left is `gitUpdateFromBase`, and the reason it is not called Pull is load-bearing: **a task branch has no upstream, so `git pull` in a worktree fails with `no upstream configured for branch 'agent/foo'`** — verified. It fetches, then fast-forwards when HEAD *is* the default branch (several agents share the main checkout; a merge there is a conflict resolution nobody asked for) and merges `origin/<default>` in otherwise. Merge not rebase, because Ship pushes early and rebasing a pushed branch means a force-push. A conflict is aborted so an agent never finds its checkout mid-merge. `defaultRemoteRef` reads `refs/remotes/origin/HEAD` — **no network**, which is what lets the worktree-creation path use it — falling back to main/master/trunk/develop for repos made with `git init` rather than cloned, where that symref does not exist.
- 2026-07-30: **Cleanup after a merge is a preference, never new git logic.** The merge modal's auto-cleanup (`SettingsFile.worktrees.autoCleanupOnMerge`, default on, mirrored by the footer checkbox) calls the same `removeWorktree()` the button calls — unforced `git worktree remove` + `branch -d`. Keep it that way: the safety is that **git refuses**, not that we checked first, so anything tempted to add `--force`/`-D` here is removing the only guard. Three things that shape the UI and are easy to get wrong if you touch it. (1) A cleanup refusal is **not a merge failure** — the commits are on the target regardless, so it renders in its own `cleanupError` warning banner ("Merged, but the worktree was kept") and never in `error`, which means the merge came apart. (2) On success `onRemove()` unmounts the modal (App drops the worktree record), so the merge handler must **return before touching state** rather than falling through to `setBusy`/`refresh` on a dead component. (3) The checkbox is the consent, so auto-cleanup deliberately does **not** re-prompt; only the manual "Remove worktree…" button confirms. Unrelated trap found next door: `AppSettings`' footer reset ran `onChange(DEFAULT_APPEARANCE)` for every pane that wasn't `agents`, so each new feature pane silently shipped a button that discards the user's theme — it is pane-aware now, and a new pane must extend `reset`/`resetLabel`.
- 2026-07-30: **Distribution is build-from-source on purpose, and the packaging tolerates a missing sidecar.** The app is ad-hoc signed (`identity: null`), so a *locally built* app opens cleanly while the same bundle moved over any network gets `com.apple.quarantine` and macOS rejects an ad-hoc-signed quarantined app with **"Chewo is damaged and can't be opened"** — not the softer "unidentified developer" prompt. That's why the README sends people to `npm run dist:install` rather than a download; `npm run dist:zip` exists for handing someone a build, and it always needs `xattr -dr com.apple.quarantine` on the other end. Only a Developer ID cert + notarization actually fixes this. Two things that make a first build survive: (1) `ensure:stt` is `--soft` everywhere now (`predev`, `predist`, `predist:zip`) because **electron-builder logs `file source doesn't exist` and skips a missing `extraResources` entry rather than failing** (`copyFiles` in `app-builder-lib/out/fileMatcher.js`) — verified by building with the sidecar moved aside, which produced a working app with only `chewo-mcp.cjs` in `Resources/bin/`. So no Swift toolchain costs you dictation, not the build. (2) `dist:install` is `scripts/install-app.mjs`, not a `ditto` one-liner: electron-builder names its output dir per arch (`mac-arm64` vs `mac`), and it refuses to copy over a **running** Chewo, which otherwise leaves a half-replaced bundle that crashes on next launch.
- 2026-07-30: **Never hand-edit a `version` field — releases go through `npm version <patch|minor|major>`.** That one command bumps the root, runs `scripts/sync-version.mjs` from the `version` lifecycle hook to propagate the number into every workspace package, stages them, and lets npm make **one** commit and tag covering all of it; `preversion` gates on `typecheck` + `test`, `postversion` pushes `main --follow-tags`. The `version` hook (not `postversion`) is load-bearing: it fires after the root bump but before the commit, which is the only window where staged files still land inside the tagged commit. `sync-version.mjs` hardcodes its workspace list — extend it when `packages/*` grows. Also worth knowing before adding version ceremony: **semver is not how Chewo identifies a build.** `src/main/app-version.ts` stamps each build with its git commit (`__BUILD_HASH__` from `electron.vite.config.ts`), diffs it against repo HEAD, and drives the sidebar's "behind by N commits" + rebuild CTA — so the version field only surfaces in the .app's Get Info panel and electron-builder artifact names.
- 2026-08-02: **Chat panes and terminal panes are two runtimes for one thing, sharing one id space.** A `TerminalTab.mode` of `'chat'` drives the agent over JSON (`src/main/chat-sessions.ts` → `ChatPane`); `'terminal'` is the pty. Ids come from `src/main/pane-ids.ts` so the tab strip, drag/drop, `paneTabs` and `MainView` never learn the difference — which is also what lets a conversation move between the two by killing one and `--resume`-ing the same `sessionId` in the other. **The terminal is not going away**: shell panes (`npm run dev`, worktree setup, the play button) are ptys forever, so "replace the TUI" would delete the fallback while keeping all the machinery. **Removed 2026-08-03: the chat pane's header** (agent badge, model, cwd, and the "Terminal" button that handed a conversation to a pty). The cwd duplicated the tab's ⎇ label and the branch chip, and a whole bar's height for an escape hatch that was never used did not earn it. `openInTerminal` went with it — the `--resume` ids still line up, so the swap is a caller away, but there is no UI for it and `claude auth` / an interactive `/config` / a broken wire format now need a shell pane. Four things about the Claude wire format cost real time to find, all verified against CLI 2.1.220 by `npm run canary:chat` (run it after every CLI update): (1) **`--permission-prompt-tool stdio` is undocumented** — absent from `claude --help` — and it is the *only* way to get permission prompts as `can_use_tool` control requests; `--permission-mode manual` just fails the tool with "you haven't granted it yet" and never asks. (2) Echoing the CLI's own `permission_suggestions` back as `updatedPermissions` is what makes "always allow" stick, so the CLI owns that state and we keep no shadow allowlist. (3) **Assistant text arrives by two routes** — as `stream_event` deltas *and* again in the completed `assistant` message — except locally-handled slash commands (`/context`), which only ever produce the `assistant` message; rendering both duplicates every reply, rendering one loses half, so `claude-chat.ts` tracks which message ids streamed. Block indices restart at 0 per message, so block ids must be `messageId:index`. (4) **`system/init` is withheld until the first turn begins**, so a fresh pane has no model and no slash commands until you have already sent something — the fix is the SDK-style `initialize` control request at spawn, which returns the full command catalog immediately. Related trap: `createChat` returns through IPC *before* the renderer mounts the pane, so nothing may be emitted synchronously from it (that event has no listener yet) — spawn-time facts like cwd reach `ChatPane` as props.
- 2026-08-02: **Whether a thinking block has readable text is per-model, and streaming cadence needs smoothing.** Verified 2026-08-02: haiku streams reasoning as `thinking_delta`s, but **opus emits the thinking block and a `signature_delta` and nothing else** — zero deltas, zero characters in the final `assistant` echo, because the reasoning is encrypted. So a thinking disclosure must check for text before offering a chevron, or it opens onto nothing (`chat-thinking-head--static` in `ChatItems.tsx`). Two neighbouring UI facts. (1) The CLI reports an **interrupted turn exactly like a failed one** (`result` with `is_error`), so `chat-sessions.ts` sets a `interrupted` flag in `interruptChat` and stamps `stats.cancelled` on the turn it stops — otherwise stopping on purpose reads as "The turn ended with an error". A tool in flight when a turn ends never receives its `tool_result`, so `reduceChat` settles any still-`running`/`awaiting` chip to `cancelled` or it spins forever. (2) Model text arrives in bursts of roughly a clause, which reads as stuttering; `useSmoothText` reveals it a few characters per frame with the step derived from the backlog, so it always catches up and never becomes the bottleneck. It snaps to full text under `prefers-reduced-motion` **and when the page is not visible** — Chromium throttles rAF for hidden windows, which would otherwise leave text stranded behind. The streaming caret is appended to the markdown *source*; as a sibling element it lands after the trailing `<p>` and sits on its own line.
- 2026-08-02: **Resuming replays nothing, and the transcript parser was silently truncating long sessions.** `claude --resume <id>` loads the conversation into the CLI's own context and then stays quiet until the next turn — no history crosses the wire — so a resumed chat pane opens blank. It is seeded from disk instead (`seedItems` in `agent-chat.ts`, fed by the same `getSession` parse the transcript view uses), from `TerminalTab.resumeSessionId` — deliberately *not* `sessionId`, which a fresh pane fills in itself and would make every live message render twice. Finding that exposed a **pre-existing adapter bug**: `parseClaudeSession` linearised by walking `parentUuid` from the newest record, but compaction and summary boundaries re-point or drop that link, and *every boundary severs the chain*. On a real long session, 964 main records yielded a **16-record chain across 58 breaks** — so both the chat pane and `TranscriptView` showed the last handful of messages and nothing else, while `meta.messageCount` (computed over the whole timeline, so unaffected) cheerfully said 488. The parse now uses the chain only when it actually reaches the first record and otherwise falls back to append order, trading rewind-branch pruning for completeness in exactly the case where the links are too broken to prune by; `tests/fixtures/claude/v2.1-compacted.jsonl` pins it. Related: the CLI withholds `system/init` until the first turn, so a pane's **model is unknown until the user speaks** — the header shows nothing rather than an indefinite "Starting…", which reads as a pane that never loaded, and the "Terminal" escape hatch falls back to the resume id so it is not dead on a resumed pane.
- 2026-08-02: **Clicking a session in the sidebar resumes it — there is no read-only transcript any more.** `TranscriptView` is deleted; `openSession` focuses the existing pane if the session is already running, otherwise calls `resumeSession`, which opens a chat pane seeded with the history. A running session's row is **dimmed** (`.session-row--running`) rather than disabled, because the useful action is still "focus that pane" — what must never happen is a second process starting on a conversation that is already live, which interleaves the file and makes the next resume pick the wrong branch. Find-in-conversation moved with it: `components/chat/FindBar.tsx` keeps ⌘F, and it paints matches with the **CSS Custom Highlight API** rather than wrapping text in `<mark>`s — the thread is live markdown that re-renders on every token, so mutating it to insert elements would fight the reconciler; ranges over text nodes survive re-render. Its Escape handler is registered in the **capture** phase so closing the find bar does not also fire the pane's Escape-to-interrupt. Two classes survive the deletion and must not be swept up as dead: `.message-markdown` (the chat thread uses it, so a live reply and a re-read one look identical) and `.transcript-error` / `.transcript-loading` (reused by `CapabilitiesView`). Open perf question, not yet a bug: a resumed pane renders every message — 539 items and ~29,000px on a real session — so a long conversation will want virtualisation before this grows.
- 2026-08-02: **A resumed pane renders one page and grows upward; the tab bar's `+` is a shell, nothing else.** Seeding a long conversation whole cost ~29,000px of DOM (539 items), so `ChatPane` withholds all but the last `PAGE` (50) and pulls earlier ones in as the user scrolls near the top — 51 items and ~2,800px on the same session. The window is tracked as a **`hidden` count from the front**, not a visible count: new items append at the end and are always shown, so nothing scrolls out of view above while the agent is talking. Two scroll subtleties, both bugs before they were fixed. (1) Prepending pushes content down, so the restore anchors on the distance from the *bottom*, and that effect is declared **before** the recorder so it consumes the gap measured on the previous render. (2) Pinning to the bottom only when items change is not enough — **markdown finishes laying out after that effect runs**, growing the scroll height underneath it and leaving a resumed pane a screen short of the newest message; the pin re-asserts on *every* render instead, which also follows the stream. ⌘F expands the window to everything first (`FindBar`'s `onOpen`), because find walks the DOM and would otherwise silently search one page. Separately, the `+` in the tab bar creates a shell directly — no menu. Agents are created from the sidebar's "New session", which already offers both CLIs, and having two launchers meant two things to keep in sync.
- 2026-08-03: **Three mutating git files now, split by blast radius: `git.ts` (read-only) → `git-ops.ts` (branch/remote) → `git-ship.ts` (commit + push + `gh pr create`).** Ship is one click with no prompts, so every decision it would ask about is a rule instead: base is the repo's own default branch (`gh repo view --json defaultBranchRef`, never a hardcoded `main`), the message comes from `git-text.ts`, and **shipping from the default branch cuts a branch first** rather than refusing — `git add -A` runs *before* the branch decision on purpose, because `git switch -c` carries the index across, which makes one `git diff --cached` the single source of truth for both the commit message and the generated branch name. Everything is idempotent: a second click pushes onto the PR the first opened (`gh pr list --head`), and a clean tree with nothing ahead says "nothing to ship". Two traps. (1) **`gh` is a Homebrew binary and Electron's PATH is not the login shell's** — the path is resolved once through `/bin/zsh -ilc 'command -v gh'` and then `execFile`d directly, so a PR body full of backticks needs no escaping; running gh *through* a shell would. (2) A failed `gh pr create` is not automatically a failed ship — the push already landed, so it re-checks for an existing PR before reporting an error.
- 2026-08-03: **Agent-written git prose lives in `git-text.ts` and is never allowed to block — but branch names are not prose.** Commit messages and PR text go through `runAgentJson` under the new `gitText` `AgentTask` (agent-agnostic, picked in Settings → Agents), each with a 25 s timeout and a **deterministic local fallback**, because a CLI mid-update must never be why a change can't ship. Branch naming was tried this way and **removed**: measured against the real CLI, "test" came back as `{"name":"test"}` — identical to the regex — for a second of latency and a spawn. It is now `src/shared/branch-names.ts` (`slugifyBranch` → `uniqueBranchName` → `branchNameFor`), pure, shared by the renderer and `git-ship.ts`, and it also sanitises: `../../etc/passwd` collapses to `etc-passwd`, never a traversal, since the result is both an argv element and a directory name. **The trap that made this visible: a headless `claude -p` writes a session file keyed on its cwd, and `assignProject` files sessions into the sidebar by that path — so running one in the repo it describes puts a "Name a git branch after this task" row in that project's session list.** Every headless feature therefore pins its cwd *outside* the user's repos (`todo-voice` → `~/.chewo/todos`, notes → the notes root, `git-text` → `~/.chewo/git-text`); Home only lists `s.project === homeDir` exactly, so `~/.chewo/*` shows nowhere. Testing gotcha that cost time: **`vi.fn().mockRejectedValue` / `mockImplementation(async () => { throw })` reports a rejection these functions deliberately swallow as an *unhandled* one**, failing the test after its assertions passed — mock the runner with a plain mutable `let answer: () => Promise<unknown>` instead of a `vi.fn()`.
- 2026-08-03: **A worktree per agent session is cheap here, and `node_modules` is the only part that isn't free.** Measured on this repo: `git worktree add` is **0.08 s** (194 tracked files, `.git` is shared so there is no second 179 MB), and `cp -cRp node_modules` is **8.2 s for 0 MB of real disk** — `-c` is an APFS clone, so it is copy-on-write and syscall-bound on ~30k files rather than on the 667 MB. `cloneNodeModules` in `worktrees.ts` runs **fire-and-forget** from the `worktree:create` handler: the pane opens the instant the checkout exists and the dependencies land while the user is still typing, which works because an agent reads code for several seconds before it runs anything. `-p` is load-bearing beyond timestamps — it preserves the **exec bit**, so a cloned `node_modules` sidesteps the npm-strips-`spawn-helper` trap that the root `postinstall` exists to repair. Failure is non-fatal by design (no `node_modules`, non-APFS volume): it toasts and the agent runs `npm install` like it would have anyway.
- 2026-08-03: **Merging into the local main checkout is gone — Ship is the only way work lands, and it goes through a review.** `mergeWorktree`, `worktreeStatus`, `landingBranchFor`, their IPC, `WorktreeMergeModal`, the `autoCleanupOnMerge` setting and the whole Settings → Worktrees tab were all deleted. Why: it bypassed review, it **never pushed** (so local main silently drifted from origin, which the origin-based worktree cutting then propagated to every new session), and its cleanup checkbox was a second mechanism for the job the merged-PR reaper already does. `worktreeState`'s "this branch is spent" detection stays — git still has to recognise a landed branch however it landed, and the test harness now lands one with a plain `git merge` rather than through the app. **`ShipModal` replaced it**, reached from the tab-bar Ship button (the focused checkout) — and, **until 2026-08-03, also from the sidebar worktree row**; that second entry point is gone as a duplicate, since clicking the row opens the branch and the tab bar's button then covers it. The per-tab icon went the same way earlier, for the same reason: clicking a tab focuses it. It keeps "Mark as done" and "Remove worktree…" in its footer. Two rules hold it together. (1) **`shipPreview` has no side effects** — it must not stage, commit or push, because it is what you read before agreeing; the cost is that its commit message is written from `git diff HEAD` plus untracked *names* rather than a staged diff, which is the right trade against leaving a cancelled review's changes staged. (2) The agent is asked **in the preview**, and `shipPullRequest` takes the confirmed `message`/`pr` as arguments, so pressing Ship is one commit, one push, one `gh pr create` with no model call in between. (3) **The wait lives on the Ship button, not in the dialog** — App reads the preview first and mounts `ShipModal` only with a finished one, because a modal that opens onto "Reading the change…" is a modal that opened too early. The read itself was the real problem: `gh repo view`, `gh pr list` and `git status` now run concurrently, and **a branch with no prior commits skips the PR-text call entirely** (its PR title *is* the one commit's subject, so asking a model to paraphrase text we already have was a second CLI spawn for nothing) — which is the common case, since every session cuts a fresh worktree. `git status --porcelain -uall` is used here deliberately (the panel's `-unormal` collapses an untracked directory to one row, which hides how much is about to be committed — the opposite of what a review needs).
- 2026-08-03: **Isolation is cut on the first message, not at spawn — and merged worktrees reap themselves.** (Default flipped to `'current'` on 2026-08-07 — see the first entry; the timing below is unchanged for a session that *is* isolated.) When `TerminalTab.branchMode` is `'separate'`, nothing is created at spawn: the worktree is named after the task, and there is nothing to name it until the user has spoken. `ChatPane`'s `beforeFirstSend` hands that first message to App, which slugs a name, cuts the worktree and **replaces the pane in place** (`replacePane`) with one whose `initialPrompt` is that same text. Four things this depends on. (1) The choice is offered **only until `sessionId` exists** — the honest "this session has started" signal for a chat pane, since the CLI only announces itself on the first turn; moving a live agent between checkouts is a different, manual act. (2) `beforeFirstSend` returning true is a promise to handle the message, so **both** outcomes end in a replacement pane — a worktree that can't be cut lands the session back in the main checkout with the reason in a toast, and no typed message is ever dropped. (3) *Superseded* — a pty pane used to have no composer to intercept, so the New session menu collected the task inline; a Codex session is now typed in a chat pane and swaps to a pty on send. (4) `cutWorktree` is the only thing that mints a `Worktree`, shared by the create modal and this path. Reaping (`reapMerged`) runs on window focus, throttled to 5 minutes, off **one** batched `gh pr list --state merged --json headRefName` per project — a call per worktree would tax every focus. Three rules make it safe to let it delete checkouts: it calls the same unforced `removeWorktree` (so **git refuses** anything unmerged or dirty), it **never touches a worktree with a live pane** (removal kills panes, and a checkout vanishing under a running agent is worse than an extra folder), and `mergedBranches` returns `[]` when gh is unreachable, so a network failure reaps nothing rather than everything. The throttle stamp is set only once there is something to ask about — worktrees load a beat after first render, and an empty first pass would otherwise spend the window.
- 2026-08-03: **A session is created undecided, and nothing is spawned until the first message.** `selectSection` opens an unstarted pane when the section has no tabs, and the sidebar's "New session" does the same; the menu that used to ask for the agent and the checkout up front is gone, and so is `startIsolatedNow`. An unstarted pane is `TerminalTab.pending`: a real tab with a composer and **no process behind it**, whose id comes from `pane:reserve` (the same `nextPaneId` counter, so the pane it becomes can take its place in the strip). Everything the setup row asks — agent, model, effort, checkout — is only answerable once you know the task, and a CLI spawned before those were settled would have to be killed and respawned on each change; `startChosenSession` builds the real pane on the first message and always returns true for a pending tab. Consequences worth knowing. (1) Choosing Codex is a runtime swap into a pty, which is why the old "a pty has no composer, so the menu collects the task inline" special case could go. (2) A pending pane has **no slash-command palette** — the catalog comes from the spawned CLI's `initialize`, so it arrives with the real pane. (3) Pending tabs are never persisted: `savedFor` filters on `!!t.sessionId`. (4) `killPane` on a pending tab calls `chatKill` for an id main has never seen, which is a deliberate no-op.
- 2026-08-03: **Interactive sessions open on the best model at high effort, and neither default names a dated id.** `NEW_SESSION_DEFAULTS` in `shared/agents.ts` is Claude → `opus`, Codex → the **head of its own discovered catalog** (`codex debug models` is priority-ordered, so entry 0 is the CLI's "latest frontier" slot — `gpt-5.6-sol` today), both at effort `high`. Deliberately *not* `AgentDef.defaultModel`, which stays "let the CLI decide" for the headless features. `sessionModel`/`sessionEffort` are the single resolver used by both the composer's picker and the spawn, so a session can never run on a model the row was not showing; effort is **clamped to the model's own `efforts`** because the set is per model (gpt-5.5 has no `max`, gpt-5.6-sol adds `ultra`) and an unaccepted level fails at spawn. `TerminalTab.model`/`effort` hold the user's override only — absent means "resolve", so a CLI update that renames a model doesn't strand a tab. Switching agent clears both (a model id belongs to one catalog). The flags diverge in `modelFlags` (`src/main/terminals.ts`): claude takes `--model`/`--effort`, **codex has no effort flag** and takes `-c model_reasoning_effort="…"`. Both values are `shellQuote`d rather than validated against an enum the way `permissionFlag` is — the accepted set is discovered at runtime, so quoting is the only guard that a catalog string reaches the command line as exactly one argument.
- 2026-08-03: **An unstarted isolated pane must not wear the shared checkout's branch.** It has no checkout of its own until its first message, so it sits in the project's main checkout — and that checkout is on whatever branch you last left it on, which is *not* this session's. Reporting it made every new session look like it had inherited an old branch (the bug that prompted all of this). `pendingBase` in App drives a dashed `.branch-chip--pending` reading "new branch · from origin/main" instead, and **Update and Ship are hidden** while it is set: both would act on a checkout the pane is only borrowing. What is deliberately *not* done is switching the shared checkout to the default branch — dev servers, the play button and every other pane are standing on it, and it fails outright when dirty. The file tree and git panel do still show that checkout's files; that is honest, since it is what is on disk there. Selecting a project also runs `gitFetchRemote` (throttled 5 min per path, fire-and-forget) — a session is cut from `origin/<default>`, so "start current" is exactly a fetch and nothing that moves a ref any checkout stands on. `git:default-base` names the base for the UI and costs nothing (local symref).

- 2026-08-03: **The in-app Update quits before it installs — `dist:install` can never run from inside the app.** `runSelfUpdate` used to shell `npm run dist:install`, whose last step is `scripts/install-app.mjs`, which dies on `pgrep -x Chewo` ("Chewo is running — quit it"). The updater *is* Chewo, so the button burned a full electron-builder run and then failed, every time, with the reason visible only in the footer's `title` tooltip. It now runs `npm run dist` and hands the swap to a **detached** `install-app.mjs --wait-for <pid> --reopen` (`detached: true` + `unref()`, output appended to `~/.chewo/update.log` — the only record once the app is gone), then `app.exit(0)` with **no `app.relaunch()`**: the helper reopens the new bundle after the swap, and relaunching here would race it into the old one. Three things the helper has to get right. (1) `pgrep -x Chewo` **also matches Electron's own GPU and utility children** — they exec the same `Contents/MacOS/Chewo` binary — so the post-exit check polls for up to 20 s instead of failing on the first read, which would abort good updates at random. (2) The copy is **staged** (`ditto` → `Chewo.app.incoming`, `mv` target → `.previous`, `mv` staged → target, restoring on failure): the slow failure-prone step happens while the installed app is still intact, and the swap is two same-volume renames. (3) `--reopen` also fires from `die()`, because a failure that happens *after* the app quit must still put something on screen. Unrelated but worth not chasing: `codesign -v --deep --strict` reports "code has no resources but signature indicates they must be present" on the installed app *and* the fresh `dist/` build — pre-existing `identity: null` ad-hoc signing, not something the swap introduces.

- 2026-08-03: **Auto-scroll is per pane, and "pinned" is a ref, not state.** Every pane stays mounted and is hidden with `display: none`, which zeroes `scrollHeight`/`clientHeight`/`scrollTop` — and every `ChatPane` re-renders whenever App does. So `ChatPane`'s run-on-every-render pin effect was firing on *hidden* conversations, writing `scrollTop = scrollHeight` = 0 (scrolling them to the top) and recording a bogus bottom-gap: one talking agent visibly moved every other chat. **Every scroll write is now gated on `active && el.clientHeight > 0`**, and the bottom-gap restore also runs on `active` changing, because a pane returning from `display: none` comes back with its offset discarded. The second bug was the stickiness itself: the restore effect had `pinned` in its deps, so scrolling up flipped `pinned` → fired the restore → `scrollTop = bottom - bottomGap`, and the gap last recorded *while pinned* is 0 — **the act of un-pinning was what slammed you back to the bottom**. `pinned` is a plain `useRef` now: nothing renders off it, deps can't fire on it, and it can be cleared synchronously from `onWheel`(deltaY<0)/`onTouchMove` — which is load-bearing during a stream, since a token can re-render between the wheel and the `scroll` event it eventually produces and re-pin under a gesture already in flight. It re-pins only by reaching the bottom again (<60px) or by sending, which is an explicit "move this on".
- 2026-08-03: **The composer accepts pasted images and long text, and the image goes to disk before it goes anywhere else.** A paste becomes a chip inside the composer pill (`.chat-attachments`, mirroring the setup row along the bottom) when it is an image or text over `PASTE_MIN_LINES`/`PASTE_MIN_CHARS` — a paragraph still pastes as a paragraph. **Verified 2026-08-03 against CLI 2.1.220** with a solid-red probe image: `--input-format stream-json` accepts a *content array* on a user message and the model genuinely sees inline `{type:'image',source:{type:'base64',…}}` blocks (it named the colour), so a chat pane needs no `--add-dir` and no Read call to look at a screenshot. Despite that, every pasted image is still **staged to a file** in `~/.chewo/attachments` the moment it is pasted, because the four ways a message can reach an agent do not agree on how to carry pixels — chat inlines base64, a codex pty takes `-i <file>`, a claude pty reads a path out of the prompt — and a file is the one representation all three derive from. That translation is `openAgent`'s job and nowhere else's; a card run does **not** use it, since `composeCardPrompt` already names its assets (SPEC-TODOS §10.2) and would say it twice. Long text is folded back into the message verbatim inside `<pasted label="…">` tags, so the chip is a display device, not a transformation. Four traps. (1) The bubble must show the chip, not the blob, which is why `deliver` in `ChatPane` takes a `display` *and* a `message` — and why `splitComposed` (the inverse of `composeMessage`) exists at all: a pane replaced on its first message is handed an already-composed prompt and would otherwise render the whole log. (2) `branchNameFor` must be fed that same `display`, or a short sentence lets the slug's five words eat the wrapper tag (`fix-this-pasted-label-text`). (3) `imageBlocks` compares `dirname(resolve(p))` against the staging dir, **not** `startsWith` — a prefix test accepts `<dir>/../../.ssh/id_rsa`. (4) An X placed on `IconButton` cannot be absolutely positioned by its own class: `.tooltip` (the wrapper IconButton renders) is itself `position: relative`, so the offset goes on `.chat-attachment > .tooltip`.
- 2026-08-03: **A branch cut from a remote-tracking ref inherits *that* branch as its upstream, which is what broke Ship.** `git worktree add -b agent/foo <dir> origin/main` leaves `branch.agent/foo.merge = refs/heads/main` (git's default `autoSetupMerge` fires on a remote-tracking start point), so `git status` in the session reports against a branch it is not, and Ship's bare `git push` dies with **"fatal: The upstream branch of your current branch does not match the name of your current branch"** — on *every* worktree session, after the commit had already landed. Two-sided fix, and both sides are load-bearing. (1) `createWorktree` passes **`--no-track`**, so a task branch genuinely has no upstream until Ship gives it one — which is what the comments in `BranchChip.tsx` and `git-ops.ts` had been asserting all along, and were wrong about. (2) `git-ship.ts` no longer treats "`@{u}` resolves" as "this branch has *its own* upstream": `tracksOwnBranch` compares `branch.<name>.merge` against `refs/heads/<name>`, and anything else falls through to `push --set-upstream`, which both pushes and repoints. That second half is not belt-and-braces — it is what repairs the worktrees created before the fix, so no migration was needed.

- 2026-08-03: **A tool result carries more than its prose, and the extra is where the UI lives.** An Edit's `tool_result` text is only "The file … has been updated successfully" — the actual patch rides beside it on `tool_use_result` (live wire) / `toolUseResult` (session file), **the same value shape both ways**, which is why one parser (`src/shared/diff.ts`, `parseToolPatch`) serves `claude-chat.ts` and `adapter/claude.ts` both. Chewo discarded that field, so the chat's edit chip could name a file and nothing else. Shapes, verified against CLI 2.1.220 on 2026-08-03: Edit/MultiEdit give `{filePath, structuredPatch: [{oldStart, oldLines, newStart, newLines, lines}]}`, Read gives `{type:'text', file:{…}}` (no patch), and a **Write that creates a file reports `structuredPatch: []`** — there is no "before" to diff, so its `content` is turned into an all-added hunk rather than rendering as "edited, nothing changed". Three things around it. (1) `tool_use_result` is per *event*, not per content block, so it is only attached when the event carries a single `tool_result` — otherwise there is no saying which file it belongs to. (2) The renderer is the git panel's, extracted to `components/DiffBody.tsx` and its CSS de-prefixed to `.diff-line`/`.diff-ln`/`.diff-code`; a patch is serialized *back* to unified text (`patchToUnified`) rather than rendered from hunks, so an agent's change and the same change read back from `git diff` are one picture with one set of line-number rules. (3) Rows are capped twice and neither cap is silent — 500 at the parser (`ToolPatch.omitted`, since a 5,000-line Write crosses IPC and becomes DOM) and 24 at the chip, which folds behind "Show N more lines"; a diff that quietly stops reads as the whole change.
- 2026-08-03: **`requires_user_interaction` means the approval card *is* the tool's UI — Allow/Deny is the wrong question and answering is the permission.** The SDK types (`@anthropic-ai/claude-agent-sdk`, `SDKControlPermissionRequest`) define it as "one-tap Approve/Deny must not be offered: the tool's approval card IS the user-interaction surface". Chewo rendered its generic "Allow AskUserQuestion?" card, and allowing ran the tool with no answers — so the model was told **"The user did not answer the questions"** no matter which button was pressed. The answers ride back inside `updatedInput` (`{...input, answers: {…}}`), and two details are load-bearing and neither is guessable: the map is keyed by the **question text**, not the `header` the card displays, and a multi-select answer is **one comma-separated string**, not an array — verified 2026-08-03, a header-keyed or array-valued map echoes into `tool_use_result.answers` and still yields "did not answer". A top-level `response` string also works and *overrides* the map ("The user responded: …"), but it is global rather than per question, so `ask-user-question.ts` does not use it and free text is just another answer string. The card only replaces Allow/Deny when the input actually parses as a question set (`parseAskQuestions` → `null` otherwise), so an unrecognised interactive tool still gets a prompt instead of an empty dialog. `npm run canary:chat` now spends a second haiku turn pinning both this and `structuredPatch`.
- 2026-08-03: **A popover pinned below its trigger is invisible in the composer — `Select` flips above when the room is up there.** The setup row (agent/model/effort) lives at the bottom of the window permanently, so `top: rect.bottom + 4` with `maxHeight: innerHeight - rect.bottom - 16` gave every one of its menus a few pixels or a negative height: the dropdowns opened and showed nothing. The rule is now `placeMenu` in `src/renderer/src/selectPlacement.ts` — DOM-free and tested (`tests/select-placement.test.ts`), like `selectFilter.ts` next to it — and it flips whenever below is short *and* above is roomier, anchoring by `bottom` so the menu grows away from the trigger rather than over it. **Do not make it unconditionally drop up**: the same bug mirrors for a settings row near the top of a modal. It also clamps `left` against the right edge, which `menuMinWidth` (260 on a trigger ~60px wide) can otherwise overhang. **There is no prettier in this repo** — no config, no dependency, no `format` script — so `npx prettier --write` fetches one with *default* settings and rewrites the whole file with semicolons and double quotes. Match the surrounding style by hand; `npm run typecheck` and `npm test` are the only formatting-adjacent gates.
- 2026-08-03: **The tab bar belongs to the tabs — everything else in it had to justify its width, and most of it couldn't.** At four sessions the right-hand cluster was ~430px of pinned controls against a strip whose tabs were `flex: 0 0 auto`, so they never shrank; they scrolled off the left edge with no fade, no arrow and no menu, and the clipped tab was indistinguishable from no tab. Four things changed and each has a rule behind it. (1) **The branch chip is deleted.** A worktree tab's label *is* its task name and its branch is `agent/<that slug>`, so the widest control on the rail restated the tab beside it — and its dashed pending variant restated the composer's own "Its own branch, from origin/main" row, on screen at the same time. Its detail moved to the tab's `title` (`tabTitle` in App) and the git panel header, which is per-tab rather than focused-only, and free. (2) **Update renders only when `behind > 0`** — which first required making `behind` mean something: git emits `# branch.ab` **only for a branch with an upstream**, and every session's branch is cut `--no-track`, so `behind` read 0 forever on exactly the checkouts whose base moves under them. `gitStatus` now counts `HEAD..origin/<default>` itself when there is no upstream and reports what it measured against as `RepoStatus.baseRef`; `defaultRemoteRef` moved from `git-ops.ts` to `git.ts` for it (a pure read, and importing the mutation file from the read-only one would have been a cycle) and is re-exported from its old home. Verify this claim before trusting any ahead/behind in this app: `git status --porcelain=v2 --branch` in a session worktree prints no `branch.upstream` line at all. (3) **▷ moved to the sidebar's project row**, where its scope is visible — it runs dev servers in the project's *main* checkout, so it never belonged to the focused session's bar; it takes a `projectId` now and selects that section, because `openTerminal` sets the view but not the section, and a shell opened for an unselected project would show a pane with no tab. `+` stayed: a plus at the end of a tab strip is a universal idiom. (4) **Tabs shrink to a 132px floor, then scroll**, with edge fades and a ⌄ menu of every session (`TabOverflowButton`), shown only while something is genuinely off-screen. Three traps in that last part. The fades are **pseudo-elements on a wrapper**, not a `mask-image` on the scrollport — a mask clips to the element box and would shear off the active tab's -1px bleed into the terminal, the same bleed the strip's `padding-bottom: 1px` / `margin-bottom: -1px` pair exists to protect. The hidden-tab count is measured with `getBoundingClientRect`, **not `offsetLeft`**, whose offsetParent is a positioned ancestor of the scrollport and so is not in the same space as `scrollLeft`. And focusing a session always scrolls its tab back into view, or the focused pane has no tab — the exact state the menu exists to rescue, reached by accident. The overflow arithmetic is `src/renderer/src/tabStrip.ts`, DOM-free and tested like `selectPlacement.ts`.

- 2026-08-03: **The chat stream carries how full the *context window* is, and nothing about how much of a rate-limit window is left.** Verified against CLI 2.1.220 (live probe + stored session logs): the numerator is on every `assistant` message as `message.usage` — and it must be `input_tokens + cache_creation_input_tokens + cache_read_input_tokens`, since a cached token still occupies the window and counting only fresh input reports *8 tokens* for a conversation sitting at 16% (a real turn here reads 32,467 cached against 8 fresh). The denominator appears **once per turn**, in `result.modelUsage[<model>].contextWindow`, so a pane knows the numerator a turn before the denominator — the readout is a token count until the first `result` and a percentage after. A turn that ran subagents lists their models in `modelUsage` and emits *their* `assistant` messages with `parent_tool_use_id` set; both are filtered out, or the reading collapses to a few hundred tokens every time a Task runs. What is **not** available: `rate_limit_event` is the whole story on limits and its `rate_limit_info` is only `{status, resetsAt, rateLimitType, overageStatus, isUsingOverage}` — no utilization, and only the *binding* window (`five_hour` in practice; the CLI also names `seven_day`, `seven_day_opus`, `seven_day_sonnet`, `seven_day_overage_included`). The percentages `/usage` prints come from a separate authenticated `GET /api/oauth/usage` the CLI makes with the user's OAuth token, and **Chewo now makes that call too** (`src/main/claude-usage.ts`) — it is the only source of a "% used" figure, so the alternative was fabricating one. Verified live 2026-08-03: with `authorization: Bearer <accessToken>` + `anthropic-beta: oauth-2025-04-20` it answers with `five_hour` and `seven_day` objects carrying `utilization` (a percentage) and `resets_at` (unix seconds) — the per-model weeklies the CLI names are simply absent when they do not apply. The token is read from the macOS Keychain (`security find-generic-password -s 'Claude Code-credentials'`, falling back to `~/.claude/.credentials.json`), used, and dropped: it is never logged, never cached, and **never crosses IPC** — only parsed percentages reach the renderer. Four rules keep this from becoming a liability. (1) **Every failure returns null** and the composer falls back to naming the window from `rate_limit_event`; nothing in the app ever waits on this call. (2) **We never refresh the token** — an expired one means the readout goes quiet until Claude Code refreshes it itself, because minting tokens against someone else's OAuth client is not our business. (3) The response shape is treated as a suggestion: `parseAccountUsage` walks the whole payload for window-named objects with a numeric utilization rather than reaching down a fixed path, and a 200 it cannot read counts as a *failure*, not as "0% used". (4) Main caches for 60 s and single-flights, so a dozen panes cost one request; the renderer forces a re-read only on the falling edge of `busy`, the one moment the number is known to have moved. **A resumed pane reads the numerator off disk** (`ParseResult.contextTokens`, the newest non-sidechain `assistant` record's own `usage`) because `--resume` replays nothing and the first live reading is a whole turn away — the window's *size* is never written to the session file, so a resumed pane says `Context 214k` until its first reply and a percentage after. `promptTokens` therefore lives in `shared/agent-chat.ts` and is used by both the wire and the parser: the two must agree, or the number visibly jumps the moment the user speaks. Measured on real sessions here, the figure tracks conversation length sensibly (39k over 8 messages, 475k over 400) — the ones above 200k are the `[1m]` Opus sessions, which is exactly why the window is read from `modelUsage` rather than assumed.
- 2026-08-03: **`pgrep -x Chewo` does not find Chewo — it finds the MCP servers, which outlive it.** `packages/chewo-mcp` is registered as *Chewo's own binary* under `ELECTRON_RUN_AS_NODE`, so every MCP server a CLI agent spawns is another `MacOS/Chewo` process; they are children of `claude -p`, not of the app, and one observed here had been alive for 30 minutes across two app restarts. Measured 2026-08-03: `pgrep -x Chewo` returned **5 pids, all of them sidecars, and not the GUI process** — so `install-app.mjs`'s running-app guard was inverted in both directions, blocking every in-app Update (`❌ Another Chewo is running`) while never detecting the app it exists to protect. The test is now an exact match on `<bundle>/Contents/MacOS/Chewo` **with no arguments**: sidecars carry the server script as argv[1], and Electron's GPU/utility/renderer children live under `Frameworks/` and carry `--type=`. What made this cost two 15-minute rebuilds to notice: **the helper runs after the app has quit, so its failures had nowhere to report** — the reopened app just showed "New version available" again, which is indistinguishable from the button doing nothing, and the only record was `~/.chewo/update.log`. `die()` now leaves the reason in `~/.chewo/update-error.txt`, which `getVersionStatus` reads on the next launch and reports as `update-failed` (cleared on a successful install, when pressing Update, and whenever the build hash catches up to HEAD). Related: the updater reads `scripts/install-app.mjs` from `__REPO_PATH__`'s **working tree**, not from the installed bundle — so a fix to it only takes effect once it is on the main checkout's branch, not when the new build lands.

- 2026-08-03: **The play button follows the focused session's checkout.** `runStartCommands` in `App.tsx` resolves the cwd from `activeTab`: an isolated, non-pending tab belonging to that project runs the start command in its worktree (`worktreeId` tagged on the pane, so `tabTitle` reports the branch and the strip label carries the `⎇` marker), everything else falls back to `project.path`. This narrows the earlier "dev servers, the play button and the `+` shell always stay in the main checkout" rule — only the `+` shell does now. Two consequences that are choices, not oversights: **ports are not remapped**, so two branches on the same dev port collide rather than one silently serving the other's build; and a run pane counts as a live pane on that worktree, which **blocks `reapMerged`** — an unattended `npm run dev` keeps a merged branch's checkout around, which beats deleting a checkout out from under a running server. A **pending** tab is excluded deliberately: it has no checkout of its own and is only borrowing the shared one, the same reason `pendingBase` hides Update and Ship.
- 2026-08-03: **`+` asks which checkout; ▷ does not.** A shell is where you check a branch by hand, and the answer is genuinely per command — `git log`/`npm test` want the session's worktree, `npm install` usually wants main — so `+` opens a two-item `ContextMenu` (the existing `ui/ContextMenu` primitive, anchored at the button's bottom-left; it already portals, clamps to the viewport and handles Escape/outside-click, so no new menu component was written) whenever the focused pane has a worktree, and stays a single click when it does not. ▷ has one obvious answer — the thing you are looking at — so it just follows the focus. Both tag the pane with `worktreeId`, which is what gives it the `⎇` label and the branch in `tabTitle`, and which counts the branch as live against `reapMerged`.
- 2026-08-04: **A tool result can be a picture, and the picture is the whole result.** A `Read` of a PNG (or an MCP screenshot tool) comes back as a `tool_result` whose content array holds `{type:'image',source:{type:'base64',media_type,data}}` and **no text at all** — so Chewo's old flatten-everything-to-`[${type}]` rule rendered the literal string `[image]` and the chip read as a tool that returned nothing. Verified 2026-08-04 against CLI 2.1.220 on both routes, which carry the identical shape: the live wire (`npm run canary:chat`, third probe — it reads a 1×1 PNG and asserts `tool_result.images`) and the session file (8 Read calls on a real transcript, 250–440 KB of base64 each). One splitter serves both, `splitToolResult` in `src/shared/tool-images.ts`, for the same reason `parseToolPatch` does. Four rules. (1) The media type is **whitelisted** to what an `<img>` paints and the payload is checked as base64, because it lands in a `data:` URL. (2) Nothing is ever dropped silently — an image past a cap leaves a `[image — not shown]` line, since a chip that shows nothing where a picture was is exactly the bug being fixed. (3) A **resumed** pane spends a `SEED_IMAGE_BUDGET` (24 MB of base64) **newest-first**, which is why `collectToolResults` walks records backwards: a session file keeps every screenshot it ever read and a resumed pane holds every item in memory. (4) The chip **auto-opens** on images the way it does on a patch, and the image is capped at 320px with click-to-full-size.
- 2026-08-06: **Ship can name a branch well because it names it *last*, and commit bodies are no longer hard-wrapped.** Two things about the generated text. (1) A session start has only the sentence you typed, so `branchNameFor` slugs it flat — but by the time you ship, a model has read the actual diff and written a conventional-commit subject, so `branchNameFromSubject` (`shared/branch-names.ts`, pure and tested) turns `feat: add oauth callback` into `feat/add-oauth-callback` for **zero extra CLI calls**: the subject was already generated for the commit. The type becomes the prefix; a scope is dropped, because `feat/api/…` reads as a directory; a subject with nothing sluggable after the type falls back to slugging the whole thing rather than emitting a bare `feat/`. It was **offered, never applied** — an existing branch got a "use …" button on the theory that its name may be one somebody is depending on. **Reversed 2026-08-09: it is applied.** Nothing can be depending on that name, because `canName` is already false the moment the branch is pushed or has a PR — and the name it overwrites is `agent/<slug of the first sentence you typed>`, which is exactly the meaningless string the ship-time name exists to improve on, so a button to opt into the better one was a click charged for nothing. The field is now derived (`branchOverride: string | null` in `ShipModal`, `null` = follow the subject) rather than seeded once, so editing the commit message renames with it; typing in the field pins it. Two edges the derivation has to keep: a subject that slugs to nothing leaves an existing branch its name but falls through **empty** when cutting, since `git-ship.ts` names a cut branch from the commit it is about to make and a fallback of `preview.branch` there would mean `git switch -c main`; and the suggestion can now collide with an existing local branch without anyone having asked for it, which main refuses by name ("A branch named X already exists.") and the editable field is the way out. (2) `commitPrompt` used to ask for a body "wrapped at 72 columns" — verified against the real CLI on 2026-08-06, that produced mid-sentence newlines (`came from\nthe task slug`), which is a `git log` convention that predates editing the message in a text box: the field soft-wraps text that is already hard-wrapped, so it reads ragged and one edited word leaves the paragraph re-flowed by hand or not at all. The prompt now asks for whole paragraphs, and `unwrapBody` re-joins them anyway — ten million wrapped commits in the training data are hard to argue a model out of. It inspects each block rather than joining blindly, so bullets, numbered lists, quotes, headings and indented code keep the newlines they need; blank lines are real structure and survive. Re-probed against CLI 2.1.220 after the change: paragraph breaks only. The **PR body is deliberately left wrapped or not as it comes** — it is markdown that GitHub renders, and it is not editable in the modal.
- 2026-08-06: **Ship's route is editable — you can name the branch and pick the target — and "one click, no prompts" survives because both are answered in the review, not asked at the button.** Ship still takes zero decisions of its own; `ShipModal` just has two more confirmed values to hand it. Four rules, three of which are about not breaking the house rules to get here. (1) **Renaming is refused once the branch is pushed.** `git branch -m` is a local ref move, but the remote keeps the old name and any PR hangs off it, so "renaming" a pushed branch means deleting a remote branch — the `--force`/`-D` territory `git-ship.ts` exists to stay out of. Main refuses it (`isPushed`) rather than trusting the UI to hide the field. (2) **`willBranch` overrides that**, and must: shipping from `main` means HEAD is a branch that is obviously pushed, but nothing is being renamed — the same field names the branch about to be *cut*, which is where naming matters most. Getting this backwards hides the input in the one case it earns its place. (3) **`protectedHead` is now `branch === base || branch === repoDefault`**, not just `branch === base`: retargeting at `develop` while standing on `main` must still cut a branch, and a PR from a branch into itself is not a thing. `branchedFrom` therefore reports the branch we were standing on, not the base. (4) **Retargeting does not re-ask the model.** The commit message describes the working tree and is base-independent; only the commit *range* moves, so `shipCompare` is a bare local `git log` and everything typed survives the change. Bases come from remote-tracking refs, never local ones — a base has to exist on the remote or `gh` rejects it. Two smaller traps: `check-ref-format --branch` **expands** `@{-1}` and reads `-f` as a flag, so both are guarded by hand *before* it (tested in `tests/git-ship.test.ts`, which covers only the pure-git parts — everything else in the file gates on `gh auth status` and would test the developer's own session); and a rename has to be written back onto the `Worktree` record in `onShipped`, because that name is what the sidebar polls state with and what `reapMerged` matches on. The worktree *folder* keeps its task name and is deliberately not moved — it lives under `~/.chewo` and nothing reads it as the branch.
- 2026-08-06: **A fresh worktree now arrives with its `.env` — and "only what git already ignores" is the entire safety argument.** SPEC §10.4 rejected auto-copying gitignored files as a secret-leak risk, with the setup command as the alternative; that was written when isolation was opt-in, and once every session cut a worktree it became a per-project tax paid before the first useful session. `copyLocalFiles` in `worktrees.ts` reverses it. Four things hold it together. (1) **Candidates are only the main checkout's *ignored* files** (`git ls-files --others --ignored --exclude-standard --directory`), never a directory walk and never an un-ignored file however the patterns are written: `.gitignore` is tracked, so it travels with the checkout and anything copied stays ignored *there* too — which is what makes it impossible for Ship's `git add -A` to stage a secret this put down. Copying an un-ignored file would instead hand Ship an untracked file to commit that the agent never wrote. `--directory` is load-bearing: it collapses a wholly-ignored folder to one entry, so `node_modules` is one row rather than thirty thousand. (2) It is **awaited** before the pane opens, unlike `cloneNodeModules`' fire-and-forget clone — these are a handful of small files, and an agent that starts against a missing `.env` reads a broken config once and then reasons from it for the rest of the session. (3) Patterns are gitignore-flavoured and live in `shared/local-files.ts` (pure, tested): no `/` matches the **basename at any depth** so `.env` finds `apps/web/.env`, `*` stops at a separator and `**` crosses it, and **last match wins** — which is the only reason the defaults can say "every `.env.*` except the committed examples" in five lines. (4) `node_modules`/`.git` are refused unconditionally and the copy is capped at 100 entries, so a `*` pattern is a bad setting rather than a disk copy. Separately, confirming what the settings pane implies: **the worktree setup command does run on the automatic path** — `startChosenSession` passes `project.worktreeSetup` into the replacement pane and both runtimes chain it (`buildCommand` → `(setup) && agent`, `createChat` → `{ setup } 1>&2 && exec claude`), so a failed setup never silently launches the agent. Its placeholder no longer suggests `cp .env .`, since that job is now done for it — and it never should have suggested `npm install` either: **the pane opens while `cloneNodeModules` is still running**, so a setup command that installs would spend those 8 seconds writing the same `node_modules` tree `cp -cRp` is filling in. Nothing in the app has ever run `npm install` (all three mentions in the tree are comments), so the collision was latent — reachable only by typing it into that field, which the placeholder invited. The clone is **atomic** now rather than incremental: it copies to a sibling of the worktree (same volume, so the rename is atomic; outside the checkout, so `git status` and Ship never see a half-copied folder) and renames into place at the end, discarding its own work if something else got there first. What actually belongs in a setup command is a *side effect a file copy cannot produce* — `direnv allow` (its allowlist is keyed on the directory path, so a new worktree is never trusted), a venv (copied shebangs point back at the main checkout), a per-branch database — never a file, which is what the copy patterns are for.
- 2026-08-04: **Every isolated branch can be deleted from the sidebar, and abandoning one is the single place `--force`/`-D` are allowed.** The trash button used to appear only on a **spent** row (merged/gone/no-branch/marked-done), which meant the one case people actually have — "I started this, I've changed my mind, get rid of it" — had no button at all, and the unforced `removeWorktree` would have refused it anyway: an abandoned branch is by definition dirty or unmerged, so "git refuses" stops being a safety net and becomes a dead end. `removeWorktree(projectPath, worktreePath, branch, discard = false)` now takes a `discard` flag that switches `worktree remove` → `--force` and `branch -d` → `-D`. **The default stays unforced and every automatic caller leaves it alone** (`reapMerged`, cleanup after a ship) — the rule is not "forcing is fine now", it is that forcing requires a person who was shown the loss. What shows them is the row's already-polled `WorktreeState`: `WorktreeRow` hands its `state` up through `onRemove(wt, state)` and `confirmRemoveWorktree` names the exact counts ("throw away 3 uncommitted files and 2 commits not on main"), passing `discard` only when there is something to throw away. Three things worth keeping. (1) That state is **polled, not live**, so an agent can write a file between the poll and the click and git refuses a removal the dialog promised was clean — the refusal is re-asked as the discard question rather than toasted, which is also how the ShipModal footer's "Remove worktree…" works (it has no state, so it passes `null` and always takes the unforced-then-escalate path). (2) The button is safe to show on live rows because `.row__trailing` is **hover/focus-revealed** already — no new CSS was needed, and the same rule is why the dirty-count badge beside it is also hover-only. (3) `git worktree remove --force` once handles a dirty checkout; twice is only for a *locked* one, which we never create, so a lock surfaces as an error instead.

- 2026-08-07: **`TodoWrite` no longer exists — the plan is a `TaskCreate`/`TaskUpdate`/`TaskList` family, and it is folded into state rather than rendered per chip.** Verified against CLI 2.1.221 by live probe (and pinned by a fourth `npm run canary:chat` turn): the tool list has no `TodoWrite` at all, so anything written from memory against it is inert code that compiles, passes tests and never fires — which is exactly how it was nearly shipped here. The shapes, all on `toolUseResult`: `TaskCreate {subject,description,activeForm?}` → `{task:{id,subject}}`; `TaskUpdate {taskId,status?,owner?,addBlockedBy?}` → `{success,taskId,updatedFields:[…],statusChange?:{from,to}}`; `TaskList {}` → `{tasks:[{id,subject,status,owner?,blockedBy}]}`. Four things follow. (1) **`updatedFields` names what changed but never carries the new value** — only `statusChange` does — so an owner or a blockedBy edge exists solely on the *input*, and `applyTaskResult` takes both halves. The join happens in `reduceChat` (via `callInput`), because the seam where `parseToolPatch` runs has the result but not the call. (2) There is no whole-list write: a plan arrives as N separate creates, so it is accumulated in `ChatState.tasks` and drawn once by `components/chat/TaskPanel.tsx`, with the feeding chips suppressed by `isPlanTool`. That set is **named explicitly and must stay that way — `Task` itself is the subagent launcher**, and `TaskGet`/`TaskOutput`/`TaskStop` are about a running subagent, so a `Task` prefix match would hide real work. (3) `TaskList` is authoritative for everything it reports but **does not echo `activeForm`**, so a reconcile carries it over by id or the running row silently reverts to its imperative subject the moment the agent lists. (4) Resuming replays nothing, so `collectTasks` in `adapter/claude.ts` folds the plan forward out of the session file (forward, not off the newest `TaskList` — most plans are never listed) and `ChatPane` seeds it only when a live turn has not already moved it. `deleted` is a status on the wire but not a state: the schema removes the task, so it leaves the list rather than rendering as a tombstone.
- 2026-08-07: **A per-pane clock must not tick while the pane is hidden.** `useElapsed` puts `Working… 1:24` on the chat's working line — "Working…" alone cannot tell three seconds from four minutes, which is the difference between waiting and suspecting a hang. Every `ChatPane` stays mounted under `display: none`, so a 1 Hz timer in each one would re-render every conversation in the window once a second for a number nobody can see; the interval is gated on `active`, and the reading is derived from a start stamp rather than counted, so a pane switched back to is immediately right instead of resuming where it left off. Same family of bug as the scroll-pin one above, and the same fix: gate on `active`. `formatElapsed` lives in `src/renderer/src/elapsed.ts` — DOM-free and tested, like `selectPlacement.ts` and `tabStrip.ts`.
- 2026-08-07: **`AskUserQuestion` is one question per page.** The tool takes up to four questions of up to four options each, every option can carry a description, and stacked that is a card taller than the window — it read as a wall rather than as a question. `QuestionCard` pages instead, with a dot stepper that fills as each question is answered and is clickable to jump back. Paging is **purely presentational**: `composeAnswers` still receives every pick at once, so the wire contract in `ask-user-question.ts` is untouched. Two rules in the interaction. A single-select pick auto-advances (picking the one answer a question accepts *is* moving on), but multi-select cannot — there is no way to know you are finished — and **the last page never auto-advances into a send**, because the tool result is always an explicit act. And since the question blocking Send can be several pages back, the disabled state names which one.
- 2026-08-07: **An isolated session's start point is a picker now, and "the default" is deliberately not stored as a ref.** The setup row's switch used to state `Its own branch, from origin/main` as a fact; the composer chip is now three controls in one pill (label, `Select`, switch) and the base is `TerminalTab.baseBranch`, passed straight to the `cutWorktree` call in `startChosenSession`. Four things hold it together. (1) **Absent means default, and default is not `origin/main`** — leaving the field alone runs `createWorktree(base: undefined)`, which fetches and then prefers whichever of `origin/<default>` and its local twin is ahead (`freshestBase`), and that resolution only makes sense at cut time. So the picker's first row has value `''`, the ref it resolves to is filtered out of the branch lists rather than offered twice under one name, and choosing it *clears* the field — which is why `setPaneChoice` builds its patch key by key instead of dropping every `undefined`: `baseBranch: undefined` has to reach the spread to clear it. (2) The list is `listBranches` (a local `for-each-ref`, no network), cached per project path in `branchLists` and re-read on every project selection — a branch created in a terminal a minute ago has to be offerable without a restart — plus an on-demand read for any project with a pending pane, since a pending pane need not belong to the selected project. A failed read is not surfaced: the picker keeps the default row, which is what it would have used anyway. (3) A chosen base **is** fetched before the cut, when it is a remote-tracking one: `origin/feature` on disk is a cache of a branch somebody else moves, so cutting from it unfetched means "as of whenever this repo last heard". `remoteOf` asks git whether `refs/remotes/<base>` resolves rather than matching on `origin/` — a remote can be named anything, and `feature/login` is a local branch, not a remote called `feature`. A local base is skipped (its ref is the truth) and the fetch is best effort like the default path's, so offline still cuts, one fetch behind. The cost is a network round-trip between Enter and the agent starting, covered by the pane's existing "Cutting a branch for this task…" notice. (4) The chip could not stay one `<button>` — a `Select` inside a button is invalid and unclickable — so the container is a `div` and both the label and the switch toggle isolation, with `.chat-setup-branch` reduced to the outline and the padding moved onto the children.
- 2026-08-07: **The merged-PR reaper was worktree-*record*-driven, so branches outlived it — `pruneMergedBranches` is the other half.** `reapMerged` walks `worktrees` (the `Worktree` records) and removes the ones whose PR landed; a branch whose record drifted away is structurally invisible to it, and records drift **by design** — dev and packaged builds keep separate `projects.json` files, and closing a pane deletes the `SavedTerminal` that was the branch's only handle. Branches made in a terminal or before the worktree flow never had a record at all. Measured on this repo: six orphaned local branches, five of them `[gone]`, against a remote that only has `main`. Three things the fix depends on. (1) **The early return had to move off the records.** It was `if (byProject.size === 0) return`, which skipped the whole pass for exactly the project the sweep exists to clean — the one with no records left. It gates on `loaded.current` instead, which is the honest signal because projects *and* worktrees arrive in the same `loadProjects()` read: before it an empty list means "not read yet" and would spend the 5-minute throttle window, after it an empty list is a real answer. (2) Every project is now asked, not just the ones holding records — which on its own put a `gh pr list` on **every project in the sidebar, including ones never opened**, on every window focus. The gate for that is `pruneCandidates`, which answers "could this project lose anything?" with **no network at all**: `for-each-ref --merged <origin/default>` minus the held set. A repo holding only its default branch, or whose extra branches are all live work, returns empty and never reaches gh — so the common sidebar costs ~4 local git spawns per project (~10ms each, concurrent) instead of a round-trip each. It is a *necessary* condition, not a sufficient one: the delete still goes through `-d`, and a stale `origin/<default>` in an unvisited project under-reports, which defers work to the next pass rather than deleting anything it shouldn't. The surviving gh calls run through one `Promise.all`, and the result is narrowed to those candidates before the prune IPC. (3) `pruneMergedBranches` **does not trust the caller's list**: it re-reads `worktree list --porcelain` and skips anything checked out (so a live agent's branch can never go), skips HEAD, and skips the `defaultRemoteRef` branch — that last guard is load-bearing and easy to miss, because once you are ahead of `main` it *is* merged into HEAD and plain `-d` would delete it (pinned by a clone-based test). The delete is `-d`, never `-D`, same house rule as the rest of `worktrees.ts`. **The blind spot is squash and rebase merges**: they rewrite SHAs, so git cannot see the branch as merged, `-d` refuses and the branch stays for a person to remove from the sidebar — identical to the gap `worktreeState` already documents. This repo merges with merge commits today, so the sweep is sufficient; switching the repo to squash-merge would silently make it a no-op. Outside Chewo the same job is a global alias, `git tidy` (`fetch --prune` + `for-each-ref` on `%(upstream:track)` = `[gone]` piped to `branch -d`) — git has no client-side post-fetch hook, so there is no way to make the local-branch half automatic in git alone, which is why it lives in the app. Note `fetch.prune=true` is already global here *and* all three of Chewo's fetch sites pass `--prune`, so stale remote-tracking refs are never the bug — they only looked stale because those fetches are throttled and best-effort.
- 2026-08-09: **A session picks one branch; Ship picks what happens to it — and the base a PR targets is the branch the worktree was *cut from*, not the repo default.** `Worktree.baseBranch` was recorded at cut time and never passed to `shipPreview`, so every session fell through to `gh repo view --json defaultBranchRef`: work started on `dev/updates` opened a PR into `main`, invisibly, until someone wondered why it never deployed. `resolveBase` in `git-ship.ts` turns a recorded start point into a PR base — the remote prefix is stripped by asking git whether `refs/remotes/<base>` resolves, **never by matching `origin/`**, since a remote can be named anything and `feature/login` is a local branch, not a remote called `feature` — and a base with no remote branch falls back to the repo default rather than failing `gh pr create` on its last step. That fallback is a **silent retarget**, which is why the modal names the target before anything is pushed. Three things around the flow it enabled. (1) **The composer's isolation switch and base picker collapsed into one control**, because they were two halves of one question; the first row (`HERE = ':current'` — `:` is forbidden in a ref, so it cannot collide with a branch) is "stay in the project checkout" and *is* the off state, and picking any branch turns isolation on. Isolation stays opt-in; that default has now moved three times and should not move again. (2) **"Continue an existing branch" was designed and then cut, and the reason is load-bearing: `git worktree add` refuses a branch already checked out somewhere.** Martin always runs several agents at once, so a session that *adopted* its branch would hard-fail the second time two of them were pointed at `dev/ui-updates`. Every session cuts a scratch branch from the pick; that is what makes concurrency work, not an implementation detail to trade away. The case it was for — "the PR has a mistake, let me fix it" — is served by picking that branch and taking the push route. (3) **`ShipArgs.route: 'pr' | 'push'`.** The direct route is `git push <remote> HEAD:refs/heads/<base>` — verified 2026-08-09 against real git: it needs **no checkout of the base**, moves **no local ref** (local `dev/updates` and `main` both stayed put while the remote advanced), and a base someone else moved is **rejected as non-fast-forward** with git's own "use git pull before pushing again" text, which `gitErrorOf` passes through verbatim. So there is no `--force` here and none is needed; the refusal is the signal to press Update and ship again. The rule for whether shipping cuts a branch first differs per route and is therefore shared, not written twice: `willCutBranch` in `src/shared/ship-route.ts` (pure, tested like `branch-names.ts`), used by `git-ship.ts` to act and by `ShipModal` to announce. It **inverts for the push route** — adding to the base is the point, so standing on it is no reason to branch, and cutting one anyway would land the commits on the remote while leaving the local branch behind them; standing on the repo default while pushing *elsewhere* still branches, because several agents are usually on it. Two consequences of the push route to keep in mind: it opens no PR, so **`reapMerged` will never see it** (hence `onShipped` marks the worktree done on either route, which is what puts the row in the state whose trash button is the way out), and pushing onto the repo default skips review entirely — the one combination that gets a warning banner, offered but never pre-selected, because bypassing review is what deleting the local-merge path in August was about.
- 2026-08-09: **Discarding a change is the fourth mutating git file, and the confirmation is the entire safety mechanism.** `src/main/git-discard.ts` joins the blast-radius split (`git-ops.ts` brings commits in, `git-ship.ts` sends work out, `worktrees.ts` manages checkouts, this destroys uncommitted work); `git.ts` stays read-only, and `safePathspec` is now exported from it because these strings reach commands that delete files — it also rejects a leading `-` now, which a read did not need. `git clean -f` is a force flag and it is allowed here for the same reason `removeWorktree(discard)` allows `--force`/`-D`: a person was shown the loss and asked anyway. What does **not** bend is that nothing here touches a commit — no `git reset`, no `git checkout <commit>` — so a discard can cost you uncommitted edits and never something the reflog could have returned. Five things that are load-bearing. (1) **The classification is re-read from git, never taken from the renderer's list.** Several agents write these checkouts continuously, so the panel's rows are a photograph: by the time one is clicked the file may be staged, may be a different kind of change, or may have been committed. A path that is no longer changed is **skipped**, because restoring it from HEAD would revert whatever happened in between — a change nobody was asked about. (2) Three kinds need three commands and one will not do: tracked → `restore --source=HEAD --staged --worktree` (both halves, or a staged edit survives as a staged change with a clean tree — invisible in the panel and still shipped by `git add -A`); staged-new (`A`) → `restore --staged` then clean, since `--source=HEAD --worktree` cannot restore a file HEAD has never seen; untracked → `clean -f -d`. **No `-x`**, so discarding a folder never takes `.env` or `node_modules` with it. (3) **A file inside a collapsed untracked directory has no status entry of its own** — `-unormal` names the folder and stops — so a child looked up by its own path is not found, and the panel's expanded folder rows would have silently skipped every discard. Caught by a test, not by reading: paths under a `/`-suffixed status entry are untracked by definition and are matched by prefix. (4) A porcelain **v1 rename is two NUL records for one entry** (`R  <new>\0<old>\0`), so the parse must consume the source path or it reads the old name as a separate file and mis-classifies the next real entry. (5) No refresh call is needed after a discard — `useGitStatus`'s fs watcher re-reads on any working-tree change — but the diff layer must be cleared if it was showing the file, or it keeps painting a diff that no longer exists. The UI deliberately shows **two icons**: a tracked file is *reverted* (`Undo2`), an untracked one is *deleted* (`Trash2`), and one icon would misdescribe half the rows. There is no "discard all" and no agent-is-working warning — the latter would fire on nearly every discard, since the panel is usually open *because* an agent just wrote these files.

- 2026-08-09: **`%(refname:short)` renders `refs/remotes/origin/HEAD` as bare `origin`, and a branch name is allowed to contain a slash — so neither list a ref belongs in can be decided from its short name.** `listBranches` (`worktrees.ts`) bucketed on `name.includes('/')` and dropped symrefs with `endsWith('/HEAD')`, and both were wrong in the base picker at once: every branch this app creates (`agent/*`, `fix/*`) was labelled **remote**, and `origin/HEAD` slipped the guard under its short name and was offered as a *local* branch called `origin` — a third row beside `origin/main` and `main` that resolves to the same commit but takes the local path through `createWorktree`, so it is never fetched. The format is `%(refname)%00%(refname:short)%00%(symref)` now: the namespace (`refs/heads/` vs `refs/remotes/`) decides the list, and a non-empty `%(symref)` is the only reliable way to recognise `<remote>/HEAD`. The old fixture could not have caught either — a `git init` repo has no `origin/HEAD` and no slashed branches, so `tests/worktrees.test.ts` now clones one.

- 2026-08-09: **Ship cuts a branch in the shared checkout and leaves HEAD on it — that is deliberate, and the cleanup belongs at the *next* "New session", not at ship time.** Shipping from the main checkout while on the repo default runs `git switch -c` (`git-ship.ts`, the `protectedHead` path) and never comes back; the reflog on this repo shows it three times, twice undone by hand, once left for weeks. The state is invisible from inside — the branch looks like ordinary work — and it silently moves where work starts, because a session that opts out of isolation opens in that checkout and inherits the branch. **Switching back inside Ship was built and then reverted**: the common next act after a ship is spotting a mistake and shipping again, which only adds to the PR just opened *while HEAD is still on its branch* — returning to `main` makes the retry cut a second branch and open a second PR for one piece of work. So the tidy-up moved to `tidyCheckout` in `App.tsx`, called from `newAgent`: starting new work is the one moment the old branch is provably finished with. It gates on `staleCheckout` (`git.ts`, read-only, no network, and it declines to report a dirty checkout), then calls `gitSwitchBranch` (`git-ops.ts` — a plain `git switch`, no `-c`, no `--force`, no stash, so it creates nothing and loses nothing; git's refusal is the guard, since the check is a photograph and an agent can write between it and the switch). It is **deliberately not guarded on live panes**, unlike `reapMerged`: that rule exists because removing a worktree deletes files under a running agent, whereas here any pane in this checkout is standing on the same *finished* branch, so moving it to the default is the favour — and with `branchMode: 'current'` the default, a live-pane guard would mean it almost never fires. The same `staleCheckout` reading feeds a `StaleCheckoutRow` under the project in the sidebar, which is the manual way in whenever the automatic path declines. **Squash/rebase merges are invisible here** for the same reason they are to `worktreeState`. What was explicitly rejected: switching the shared checkout unconditionally when a session opens — several agents, dev servers and the play button stand on it, and a switch with no merged-and-clean gate either fails on a dirty tree or carries somebody else's work onto the default branch.
- 2026-08-09: **A typo'd design token is not a fallback, it is a dropped declaration — `--space-10` does not exist.** The scale is 2/4/6/8/12/16/20/24/32 (styles.css line ~91, several per line, which is why a careless grep for the definition finds nothing and a careless grep for *uses* looks fine). An unresolvable `var()` makes the whole property invalid *at computed-value time*, so `padding: var(--space-8) var(--space-10)` does not fall back to the 8 — it falls back to the initial value, **0**. That is what jammed the Ship modal's route chips ("Open a pull request" / "Push straight on") against their own borders; the two `margin-bottom: var(--space-10)` beside it had simply never applied, and the section's `gap: var(--space-6)` had been doing all the spacing for months. Two things worth keeping: the audit is `grep -o -- "var(--[a-z0-9-]*)"` minus `grep -o -- "--[a-z0-9-]*[[:space:]]*:"`, both sorted and `comm`'d, which finds every phantom token in one pass (it now reports only `--border-subtle`, used once at line ~5232 and also undefined); and **pair every `font-size` token with its `--lh-*` twin** the way the rest of the file does — the chips had neither, so their height was whatever the inherited line-height happened to be. There is no stylelint and no prettier here, so nothing but that grep will ever tell you.
- 2026-08-09: **Ship's commit message was the *fallback* almost every time, and the fallback is indistinguishable from an answer — but the text was bad for a second, worse reason: the diff was sliced, so the model never saw the code.** Two independent defects behind one symptom (`chore: update 4 files`, and before that a confident subject about the wrong change). (1) **`TIMEOUT_MS` was 25s and every model is slower than that.** Measured 2026-08-09 against CLI 2.1.220 on an 8.6 KB prompt: the CLI's own default model took **25.4s** run inside the repo and **50.9s / 60.2s** from `neutralCwd()`, sonnet **33.8s**, haiku **12–32s** (wildly variable, and its subjects were both vaguer and sometimes invented — "add Ship route picker" for a diff that adds no picker). The one combination that is fast *and* repeatable is **`--model opus --effort medium`: 13.8 / 17.7 / 13.9s**, which is what `FAST_TEXT` pins when the Settings choice leaves model/effort blank — for this task an omitted model is **not** "let the CLI decide" the way it is for notes and todo-voice, because Ship *blocks a click* on it. An explicit Settings choice still wins, and codex is left alone (its catalog is runtime-discovered, so no id can be hardcoded honestly). The timeout is now 90s. Note the naive intuition is backwards: the neutral cwd is *slower* than the repo, not faster — and a run inside the repo can look deceptively good because the CLI has file tools and just reads the files the diffstat names, which the real (neutral-cwd) call cannot do. **Always measure this feature from `~/.chewo/git-text`.** (2) **`diffText.slice(0, MAX_DIFF_CHARS)` is a lottery held on filenames.** `git diff` emits files in path order, so on a four-file change here `AGENTS.md` sorted first and consumed all 8,000 characters alone — the model was shown zero lines of `App.tsx`, `ShipModal.tsx` or `styles.css`. An *appended* hunk makes it worse than it sounds: the context lines around the change are that file's own prose, so the model reads paragraphs describing **other commits' work** and summarises those, which is exactly how every model produced a fluent, wrong subject. `budgetDiff` (exported, tested) splits on `/\n(?=diff --git )/` and gives each file an equal share, small files handing their remainder back to the ones still over. Two things it must keep doing: **spend the truncation notice out of the file's own share** (added on top it breaks the cap once per truncated file — caught by a test, not by reading), and cut on a line boundary so a half-written hunk header never reads as corruption. With the same model and the same 8,000 budget, the subject went from "move ShipModal presentation into its own styles" to naming the actual fix. Worth knowing for anything else that feeds a diff to a model: **a truncated diff does not look truncated to the reader**, so it produces confidence rather than hedging. (3) With the lottery fixed, the cap itself was still too low, and the thing that justified 8,000 turns out not to be true: **prompt size costs no latency here.** Measured on this repo's largest commit (93 KB, 21 files), same model, budget split evenly: 21.8s at 8 KB, 17.2s at 24 KB, 16.9s at 48 KB, 15.8s at 96 KB — the wait is CLI startup plus the message being generated, not the diff being read, so starving the model buys nothing. Quality does move: at 8,000 (≈380 chars, five lines per file) it saw only the first of that commit's two changes and shipped a subject that silently omitted the other half; from 24,000 up it named both. `MAX_DIFF_CHARS` is **48,000** now and `MAX_STAT_CHARS` (was a bare `4_000` written twice) is 12,000 — the diffstat is the cheapest signal there is, one line per file, and it is what carries a big commit. 96,000 was marginally sharper again and is left on the table on cost grounds, not on latency ones; revisit that if a Ship message ever reads as half a story.

- 2026-08-09: **The base picker's rows were mislabelled by omission — `main` there means "cut a scratch branch *from* main", never "check main out".** Collapsing the isolation switch into the picker (entry above) left the two modes styled identically, so the off state read as a fourth branch and the branch rows read as a checkout switch; the only place the verb appeared was the container's `title`, which is on the outer div and so is unreachable while the menu is open. `Select` takes an optional `SelectOption.group` now and draws a heading whenever it changes between two consecutive *shown* rows — recomputed after filtering, so a heading can never strand over an empty section or repeat — and the composer uses exactly two: **In this checkout** (the `HERE` row, detail "shared — other agents write here too") and **New branch from…** (everything else). Three things to keep. The header is `role="presentation"`, because keeping the rows a flat list is what lets arrow-key nav stay indexed on `shown`. The "not found" fallback row spliced in at index 2 must carry the same group as its neighbours or the heading draws a second time under it. And grouping is a *label*, not a sort — `Select` renders in the order given. Second half: **`listBranches` now drops the local twin of the default base when it is on the same commit.** A fresh clone offered `origin/main` (the default row), local `main`, and the checkout itself — three rows, one commit. It is dropped on **commit equality only** (`%(objectname)` joined to the refname), never on name: a local `main` holding unpushed commits is a genuinely different start point. It is also the *only* pair that can be collapsed this way — `resolveBase` strips the remote prefix, so `origin/main` and `main` produce the same PR base, whereas local `feature` and `origin/feature` at one commit still target different bases and both stay. The remote's name comes off the refname (`refs/remotes/<remote>/…`), never from an `origin/` prefix match, same rule as `resolveBase` and `remoteOf`. `current` is still reported for a branch dropped from the list.

- 2026-08-09: **A word budget spent on punctuation names nothing — `slugifyBranch` swallows the apostrophe and packs whole words, and a subject is cut at its first clause.** The commit `feat: label base picker's two modes, drop default's local twin` shipped on `feat/label-base-picker-s-two`, and three separate rules produced it. (1) The character class replaced `'` with a **space**, so `picker's` split into two words and the orphaned `s` was both noise and one of the five slots — an apostrophe is an elision, not a boundary, so it is now dropped before the class runs (`picker's` → `pickers`, `don't` → `dont`). (2) The 48-char cap was `join('-').slice(0, 48)`, which **cuts mid-word**; it packs whole words within the budget instead, since a truncated word reads as a typo rather than as a limit. That path was latent — five short words never reach 48 — and only fires on long ones. (3) The five-word cap is right for a session start, where the input is a sentence someone typed, but a commit subject is **already a summary**, so `branchNameFromSubject` gets `SUBJECT_WORDS = 8` and `describeChange`, which slugs the first clause (split on `,;—`) and falls back to the whole subject when that clause has no hyphen in it — one word is a worse name than a long one. A subject describing two changes otherwise stops mid-sentence and names neither half. Result: `feat/label-base-pickers-two-modes`.
- 2026-08-09: **"Finished with this branch" is *all pushed*, not *merged* — gating `staleCheckout` on `merge-base --is-ancestor <branch> origin/<default>` meant the tidy-up fired for almost none of the workflow it was written for.** Ship parks the shared checkout on the branch it cut and opens a PR; that PR is unmerged for as long as review takes, so the merged-only gate returned `null` at exactly the moment a person clicks "New session" wanting their checkout back — and even after the merge it needed a fetch to move `origin/<default>`, by which time the branch had been switched by hand. This repo's reflog shows the manual switch happening 54 seconds after the ship. The test is now `git rev-list --count HEAD --not --remotes` = 0: every commit on the branch is reachable from *some* remote-tracking ref. One reading covers all three ways work leaves — the PR route (`origin/<branch>`), the direct push route (`origin/<base>`), and a branch whose PR has since landed — and it no longer depends on `origin/<default>` being fresh, because a just-pushed branch is recognised from its own remote-tracking ref. **Zero unsent commits plus a clean tree is the entire safety argument**: switching away cannot cost anything, since the branch stays exactly where it is and every commit on it exists somewhere else. `merge-base` survives only to word the toast and the sidebar row (`StaleCheckout.reason: 'merged' | 'pushed'`), so a failure there reads as the weaker claim rather than blocking the switch. Two things this deliberately does **not** do: it does not use `@{u}`, because Ship's push route sets no upstream on the local branch (it pushes `HEAD:refs/heads/<base>`), so an upstream test would miss half the cases; and it still cannot see squash or rebase merges, which rewrite SHAs and leave the local commits on no remote ref — same blind spot as `worktreeState`. The cost accepted: a long-lived feature branch you pushed and are still using in the shared checkout will now be tidied away when you open a new session. That is lossless and announced by toast, and the alternative was a feature that never fired.

- 2026-08-13: **A link in a chat reply used to eat the app — the main window had no navigation guard at all.** `createWindow` set neither `will-navigate` nor `setWindowOpenHandler`, and none of the five `ReactMarkdown` sites overrode `a`, so markdown emitted a plain `<a href>` and clicking one navigated the BrowserWindow itself. With `titleBarStyle: 'hiddenInset'` there is no back button and no close: the page sat on top of the whole UI until ⌘R. The `open-external` IPC had existed the whole time and had exactly **one** caller (the Ship toast's "Open PR"). Fixed in two layers, and both earn their place: `MD_LINKS` (`src/renderer/src/markdownLinks.tsx`) answers the click so no navigation ever starts, and main pins the frame as the backstop for everything markdown is not. Three traps in the backstop. (1) **The one navigation that must survive is the renderer reloading itself** — ⌘R and Vite's HMR full reload are renderer-initiated and come through `will-navigate`, so a blanket deny bricks dev; it is a whitelist of the page we loaded. (2) That whitelist cannot be `startsWith('file://')`, which was the first draft and would have let `[x](file:///…)` navigate a *packaged* build after all — dev matches the server by origin (its URL carries HMR query strings), prod compares against `pathToFileURL(...index.html)` with the query/hash stripped. (3) `openInBrowser` is http(s)-only for the reason the old IPC comment gave: any other scheme hands the OS a string that launches an application of its own choosing, and these strings come from model output. Considered and not built: an in-app browser pane (a `WebContentsView` pane mode beside `chat`/`terminal`) — the default browser is where a link belongs, and that pane is a real build with its own sizing, chrome and security policy.

- 2026-08-17: **Codex has subagents now — SPEC-CAPABILITIES.md §1/§2 and the `AgentRef` comment ("CC only") are stale.** Verified on this machine: `codex features list` reports `multi_agent` as **stable, enabled** (codex-cli 0.144.5), and the binary carries `developer_instructions` (required, and it rejects a blank one), `agents.max_depth`, `agents.max_threads`, `agents.job_max_runtime_seconds`, and a `features.multi_agent_v2.*` block (`max_concurrent_threads_per_session`, the wait timeouts, `subagent_usage_hint_text`, `hide_spawn_agent_metadata`). Its own warning string says subagents "can increase usage quickly. Consider setting … below 8." Per the docs, definitions are **TOML** in `~/.codex/agents/` and `<proj>/.codex/agents/` — `name`, `description`, `developer_instructions`, plus any `config.toml` key (`model`, `model_reasoning_effort`, `sandbox_mode`, `mcp_servers`); neither directory exists here yet, so **verify the loader path against the installed binary before writing to it**. Claude's half is `<proj>/.claude/agents/*.md` and `~/.claude/agents/*.md`, markdown with YAML frontmatter, and the body is the *entire* system prompt — a subagent does not inherit Claude Code's own. Three consequences. (1) **These are not portable the way `SKILL.md` is.** That cross-agent standard is why skills copy unmodified; agent definitions are two formats with different semantics, and the tool policy especially does not map (Claude names tools in `tools`/`disallowedTools`, Codex sets `sandbox_mode`), so "install to both" is a lossy translation and must be labelled as one. (2) The **`description` field is the router** — it is what the main agent reads to decide whether to delegate, so it is load-bearing prose, not a caption. (3) **Doc/build drift is real**: the docs name `agents.default_subagent_model` and that string is absent from the 0.144.5 binary — treat the published field list as a hypothesis and pin it with a canary the way `npm run canary:chat` pins the wire format. Related auth note, latent rather than broken: `buildPtyEnv` scrubs `CLAUDECODE`/`CLAUDE_*` but not `ANTHROPIC_API_KEY`, and every pane spawns through `/bin/zsh -ilc` — an exported key in the user's shell rc would silently move the whole app, subagents included, off the subscription and onto pay-per-token billing. Not set on this machine; worth detecting and warning about rather than scrubbing, since some users mean it.

- 2026-08-17: **The agent builder is one CLI call and four pure modules, and the split is forced by what can import electron.** `main/agent-builder.ts` holds only the spawn — timeout, neutral cwd, model pin — because it reaches Settings through `agentFor('agentBuild')` and `settings.ts` imports `electron`, which puts anything in that file out of reach of both the renderer and a canary (`tsx` dies with "does not provide an export named 'app'"). Everything else is shared and pure: `capabilities/agent-file.ts` is the on-disk format (serialize/parse/sanitize), `capabilities/agent-draft.ts` is the builder's request, JSON Schema, prompt and the reconciliation of what comes back, and `capabilities/frontmatter.ts` is the parser **extracted out of `scan.ts` for exactly this reason** — `scan.ts` reads directories and so imports `node:fs`, so the agent editor previewing a file would have pulled the filesystem into the renderer bundle (same trap `shared/chewo-mcp.ts` documents). Five things measured or decided here. (1) **Latency is 41–72s** at opus/high for a 4–6 KB system prompt (`npm run canary:agent`, CLI 2.1.233, two runs) — so `gitText`'s 90s would kill a working call about half the time, and `TIMEOUT_MS` is 180s. Erring long is free because there is **no fallback**: unlike a commit message, where a deterministic local string beats a slow model, a locally-invented system prompt would be worse than an honest failure, so a failure is reported rather than papered over. For the same reason the blank-model pin is the *opposite* of `gitText`'s — `CAPABLE` is opus/high, not opus/medium, since this runs once per agent and quality dominates. (2) **`preload` is forced off whatever the model says.** The `skills:` frontmatter preloads a skill's full body at startup on every invocation (~50,000 tokens for figma's 12 against ~500 for their descriptions), so it is the user's call on the review screen; the drafting model only proposes *which* skills, each with a `reason` that is shown and **never written to disk**. (3) A skill name the model invented is **dropped**, not carried — an unknown name in `skills:` is a preload that fails at spawn, and nothing in the UI can distinguish a hallucinated skill from an uninstalled one. (4) **Overwriting is the edit path**: `writeAgent` reads the existing file and hands it to `serializeAgent`, which carries every unmodelled frontmatter key (`permissionMode`, `maxTurns`, `hooks`, whatever a CLI update adds) through in its original position — the failure mode without it is silent, since the agent still loads. That is also why the frontmatter is rewritten line-by-line rather than through a YAML round-trip, which would renormalise quoting and ordering across lines the user never touched. (5) `tools: []` and an omitted `tools:` are **opposites** — omitted grants every tool, `[]` grants none — so an empty allowlist must never be emitted, and `toolPolicy` in `CapabilityRows.tsx` must never render one as a restriction.

- 2026-08-17: **Three bugs in the agent builder's review screen were invisible to typecheck, 596 tests and a clean build, and all three fell out of driving the app.** (1) `capabilities:readMemory` **refuses anything that is not CLAUDE.md/AGENTS.md**, so the Edit button failed on every agent with "refusing to read non-instruction file". The fix is `readAgentFile`, a second narrow door (a `.md` whose parent directory is literally `agents`) — *not* a widened whitelist, because that guard is the only thing stopping an IPC channel named "read a file" from becoming one. (2) The preview pane rendered `serializeAgent(draft)` with no second argument, so it showed a file **missing every unmodelled frontmatter key** — `permissionMode`, `maxTurns`, `hooks` — while the actual save preserved them, directly under a subtitle reading "Nothing it does not show is lost". The modal now takes the file's original text as `edit.source`; a preview that disagrees with the write is worse than no preview. (3) A disabled plugin's skills were labelled **"not installed"**, which is simply false — they are on disk and reach nobody. `SkillOption.unavailableReason` carries the true words ("the figma plugin is disabled") and the row and the warning both use it. Two layout notes from the same pass: `ModalShell` grew an `xwide` (880px) step because `wide` is 640px and a subagent's system prompt wraps to about seventy monospace columns there, and any flex row nested inside a modal field needs `width: 100%` or it sizes to max-content and overflows — same trap as `.capability-row__line`. The GUI was driven over CDP with the `ws` already in `node_modules` (`npx electron out/main/index.js --remote-debugging-port=9787`); **ports 9222 and 9333 are squatted on this machine** by an unrelated Chrome. Screenshot the result and look at it — bug (2) was legible only as two missing lines in a `<pre>`.

- 2026-08-17: **Installing an expert agent does not make it get used — delegation is a judgement, not a routing table, and it loses to "I can just do this".** Verified live: a session with `figma-expert` installed *and* the Figma MCP tools available called those tools itself and never delegated, and `claude -p` confirms the agent was loaded and offered the whole time. The `description` is only a suggestion the main model reads; a subagent wins that judgement when it offers something the main agent lacks (a clean context, a narrower tool policy), and when the main session already holds the same tools it offers nothing. So Chewo has a **Lead toggle** on the composer's setup row: `orchestratorBrief` (pure, `shared/orchestrator.ts`) is passed with `--append-system-prompt` alongside `--forward-subagent-text`, and the roster is resolved at spawn by `main/roster.ts`. Six things hold it together. (1) **It is opt-in and must stay so** — delegation costs a fresh context that cannot see the conversation, which is the wrong trade for the small edit most sessions are; the brief therefore carries a "when not to delegate" half, without which a one-line CSS fix becomes a three-agent fan-out. (2) It is **spawn-time**, like the checkout picker, so it is offered only while the pane is `pending` — `--append-system-prompt` cannot be added to a live session. (3) An empty roster returns `''` and the flag is not passed at all: briefing a lead to dispatch to nobody produces an agent that announces a plan and then does everything itself, which reads as the feature being broken. (4) Agents in **disabled** plugins are excluded — the CLI will not launch them, and naming an agent that cannot start is worse than omitting it. (5) `listInstalledPlugins` is now cached 30s and single-flighted because it is a process spawn and sits on every project selection; **agent files are deliberately not cached**, since writing one and using it in the next session is the point. (6) Attribution comes from `launchedAgent` (`shared/subagent.ts`), which reads `subagent_type` off the `Agent`/`Task` call — matched by exact name, never by prefix, because `TaskCreate`/`TaskUpdate`/`TaskList`/`TaskGet`/`TaskOutput`/`TaskStop` all start with `Task` and launch nothing. Identity is two initials plus a hue (`shared/agent-identity.ts`): FNV-1a rather than a character sum, because a sum gives near-identical names neighbouring hues and those are exactly the ones needing to be told apart. Note the CLI's built-in agents (`Explore`, `Plan`, `general-purpose`) are not files and so never appear in a scan-derived roster — the count in the toggle is *your* agents, which is the honest number.

- 2026-08-24: **A light theme is a direction flip in three derived ramps, not a second stylesheet — and the CSS block that finishes the job can only hold tokens JS never writes.** `isLightBase` (base lightness > 55) is the single switch, exported from `shared/appearance.ts` so the surface math, `applyAppearance`, `makeEditorTheme` and the preset gallery never disagree about which way round the app is. Four things it turns. (1) `deriveSurfaces` subtracts its offsets instead of adding them, so a raised surface *darkens* — the GitHub-light shape, white window and grey hover, which also keeps hover/press reading as "more emphasis". (2) `deriveTextRamp` swaps its cream anchors for ink ones (`#202326 / #474d55 / #6a737f / #959da6`), picked to match the cream ramp's own contrast on graphite level for level rather than to match anybody's hex. (3) `deriveRim` goes to alpha 0.7 — a white hairline at 0.05 is invisible on a light card. (4) **A light ramp holds *chroma* steady rather than HSL saturation**: HSL compresses saturation near white, so Solarized's `#fdf6e3` measures s=87% while its own next surface `#eee8d5` is s=42%, and carrying s down the ramp turns a cream into custard. Dark ramps deliberately keep s flat — the shipped dark presets were authored against that and Nord's real ramp does the same, so this correction is light-mode only. Two traps around the CSS. **`applyAppearance` writes inline styles on `documentElement`, which outrank `:root[data-theme='light']`** — so that block may only hold tokens the function never sets (shadows, scrim, danger/warning, badges, diff, and the accent hover/press/text steps, which have to run *down* on a light surface); putting `--c-live` or `--c-project` there does nothing, because both are written inline from the preset's own accents. And **`--c-on-accent` cannot be a fixed dark ink**: `inkOn()` scores the accent's own near-black and near-white twins by contrast and picks the winner, which is what stops a deep accent (a light theme's saturated blue) from rendering its button label at 2:1. Preset note: **GitHub Light survives the 3:1 bar untouched; Catppuccin Latte and Solarized Light do not** — several of their own colours sit under it on their own paper, and both were built, screenshotted and then dropped rather than shipped darkened under their names. `tests/appearance.test.ts` pins the flip, the chroma rule and the per-slot contrast; the ANSI bar deliberately skips `black`/`brightBlack`/`white`/`brightWhite`, since the ends of that ramp are low contrast by definition (the stock graphite preset's own `black` sits at 1.1:1).

- 2026-08-24: **The three accents are three signals, not a colour harmony — and picking them out of one palette family is what broke them.** `accent` means you-are-here, `accentSecondary` paints the expanded project's wash and left bar (this-container-is-open), `accentTertiary` paints every live dot, running row and in-flight tool chip (an-agent-is-working). Measured across the shipped presets, **five of seven put the live colour within 30° of the accent — the default among them** (emerald h156 against a cyan h185), so a running session read as a slightly bluer focused one; Nocturne was three shades of one colour (17°/14°/31°) and its secondary only cleared 4.4:1. Two rules now hold, both pinned in `tests/appearance.test.ts` so a new preset cannot quietly reintroduce it. (1) **All three at least 45° apart in hue**, which is why the default's live is now magenta `#ef7fc8` rather than the cyan it was. (2) **Clear of the hues the fixed status colours already own** — danger/diff-del (h5), the Claude badge (h17), warning (h38), diff-add (h135), the Codex badge (h214) — since landing on one makes two different things the same colour. Those exclusions leave exactly three usable bands, **64–109, 161–188 and 240–339**, which is a tighter budget than it looks: an emerald accent at h156 rules out the middle band entirely, so its live colour *cannot* be the conventional cyan and stays pink. The emergent convention is secondary = the theme's cool violet/teal, tertiary = a vivid magenta-pink — except where the accent already owns pink (Nightshade, Rosé), where live takes the turquoise slot instead. Nocturne is the other exception: Nord's aurora set has only purple and green far enough from its frost accent, so live is `#a3be8c`. **There is no migration**: `loadSettings` deep-merges over `DEFAULT_APPEARANCE`, so anyone whose `settings.json` already holds an explicit `accentTertiary` keeps it, and the fix only reaches them when they pick a preset or edit the two colours in Settings → App. That is deliberate — silently rewriting a colour somebody chose is worse than leaving it.

- 2026-08-24: **Driving the app with `npx electron out/main/index.js` writes to `Application Support/Electron-dev`, not to the installed app's store — but the installed app's own store is `Application Support/chewo`, lowercase, and macOS is case-insensitive.** So the earlier "dev and packaged builds keep separate `projects.json` files" is true, and this is *why*: a bare `electron <entry>` run takes Electron's own app name, not the one in `package.json`. Two things follow for anyone screenshotting a UI change over CDP. (1) You can click through Settings freely — theme changes, added projects, cleared project lists all land in `Electron-dev` and the user's real Chewo never sees them; there is a stale `chewo-dev` directory from an older convention that nothing writes to any more. (2) That also means **the dev instance does not reproduce the user's own settings**, so "it looks right here" is a statement about the defaults, not about what they will see — check their `settings.json` before claiming a themed change reaches them. Verified by mtime and by reading all three files while the packaged app was live.

- 2026-08-24: **Setting a font is two edits; making it actually reach the whole app is four.** The UI stack is Plus Jakarta Sans at weight 400, self-hosted from `src/renderer/src/assets/` (`fonts.css` + four woff2), imported at the top of `styles.css` and linked from `hud.html` — the voice HUD is a separate entry with its own inline `<style>` and no stylesheet import, so a font change that only touches `styles.css` silently misses it. Vite hashes the woff2 into `out/renderer/assets/` for both entries off one copy; verify with `grep -o "url([^)]*)" out/renderer/assets/*.css` after any change, since a broken path fails to a fallback face rather than to an error. Three traps. (1) **`body` had a hardcoded `-apple-system` stack**, not `var(--font-ui)` — the token existed and the body ignored it, so the stack was set in two places and only one of them was the one that mattered. (2) **Form controls do not inherit `font-weight`.** The UA sets it on `button`/`input`/`textarea`/`select` by element selector, and the repo hands them `font-family: inherit` one rule at a time without ever handing them the weight — so any resting weight other than 400 stops at their border and they read a shade off the text beside them. One reset near the top of `styles.css` fixes it; do **not** reach for `* { font-weight }`, which beats the UA's `strong`/`b`/`h1` rules and kills every bold in the app. (3) **Light 300 was tried and reversed the same day.** Every heading above 15px here is serif (`--font-display`), so the sans face only ever renders at 11–15px and there is no display size for a display weight to live at; and the change is width-neutral — measured in-app, one sidebar title is 163px in SF Pro 400, 164px in Plus Jakarta Sans 400 and 163px at 300 — so weight buys no density and costs only stem contrast, which small grey text on a white base cannot spare. The resting weight is `--weight-body`, deliberately *not* a redefinition of `--weight-regular`: one token moves the whole app and the ramp keeps meaning what it says. Checked on screen over CDP: `document.fonts.check('300 13px "Plus Jakarta Sans"')` is the honest probe, because a computed `font-family` naming the family proves only that the CSS parsed.
- 2026-08-24: **The coding view uses sidebar-only agent-session navigation.** There is no main session-tab strip; a pending pane appears immediately as **New session** beneath its project, and the focused header shows the session name plus its branch/worktree truth. Files, Git, and auxiliary shells share one switchable tools panel at the **top-left of the coding workspace**, between the project/session sidebar and the conversation; Files keeps its explorer and editor together, file tabs preview on single click and pin on double click, the explorer can collapse, and Run opens a tabbed shell there rather than another agent-session surface. Persist the project-sidebar, tools-panel, and explorer widths plus explorer collapse state.

- 2026-08-25: **The tool picker lives in the session header, and the explorer's own button collapses rather than closes.** Two fixes to the coding workspace, both about width. (1) The vertical activity rail cost 42px of every workspace row for three icons that are only ever clicked when switching tools; they moved into `SessionHeader` as a `leading` slot (`components/tools/ToolActivityBar.tsx`, was `ToolActivityRail.tsx`), which is a row that already exists and has spare width. `ToolsPanel` is now just the open tool's body — when `activeTool` is null nothing renders in that column at all, so `ACTIVITY_RAIL_WIDTH` and the `tools-column-collapsed` branch are gone. (2) The file explorer's header button was an X labelled `Hide files (⌘⇧E)`, which named the shortcut that closes the **whole Files tool** while the button itself only set `explorerCollapsed` — two different actions wearing one label, and the X read as the destructive one. It is a `PanelLeftClose` icon labelled `Collapse explorer (⌘⇧B)` now, matching the `PanelLeftOpen` on the collapsed rail, and the prop is `onCollapse` rather than `onClose`. **⌘⇧B is the new keybind and ⌘B was deliberately not used**: Ctrl+B is readline's backward-char and tmux's prefix, and the handler matches `metaKey || ctrlKey` like its ⌘⇧E/⌘⇧G neighbours, so a bare-B binding would steal a key every terminal pane needs. It is gated on `activeToolRef.current === 'files'` — collapsing a tree nobody can see is a keypress that does nothing visible.
