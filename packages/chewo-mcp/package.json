{
  "name": "@chewo/mcp",
  "version": "0.1.0",
  "description": "Chewo's MCP server — exposes Claude Code + Codex session history, handoffs and todos as tools; the shared memory both agents query",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/index.cjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.76"
  }
}
